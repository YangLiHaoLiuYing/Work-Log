
  work-log 演示 · 临时目录 <临时目录>
  （所有输出都是这套 CLI 的真实输出，没有美化过）

────────────────────────────────────────────────────────────
① 看门狗：抓到静默卡死
   三个 agent 都在写心跳，agent3 写到一半不吭声了。
   为了让演示不用等 45 秒，这里把卡死阈值压到 3 秒（--stale-after 3）。
   ...agent3 之后再也没有写过心跳，等 4 秒...
   （agent1 / agent2 继续在写，只有 agent3 停了）

work-log · 03:32:30 · <临时目录>/stuck/board.md
任务：演示：谁卡死了   心跳周期 15s   卡死阈值 3s
上次扫描：03:32:30（0s 前）
------------------------------------------------------------------------
agent1       心跳中    最后 03:32:30  静默    0.3s  条目   2
agent2       心跳中    最后 03:32:30  静默    0.2s  条目   2
agent3       疑似卡死   最后 03:32:25  静默    4.5s  条目   1  <= 疑似卡死
------------------------------------------------------------------------
用户喊话 0 条 · 未确认告警 1 条
  #1 [03:32:30] <agent3> 静默 4s 未确认

⚠ <watchdog> 03:32:30 [告警] <agent3> 已静默 4s（阈值 3s）且未写「任务完成」→ 疑似卡死，请在线 agent 检查该 agent 是否卡死并修复

   ↑ 只有 agent3 被标红，agent1/agent2 静默 0s 属于正常。
     check 有卡死时退出码是 1（业务码），可以直接写进 CI / 脚本判断。

────────────────────────────────────────────────────────────
② 等待图：抓「心跳全绿的死锁」
   这是 work-log 独有的能力。场景：agent1 和 agent2 互相问了对方一个问题，
   然后两边都阻塞等对方回答 —— 双方心跳都很勤（await 会持续续心跳），
   看门狗看它们是「最健康的两个人」，但整个团队其实已经死了。

work-log · 03:32:34 · <临时目录>/deadlock/board.md
任务：演示：互相等待   心跳周期 15s   卡死阈值 45s
上次扫描：03:32:34（0s 前）
------------------------------------------------------------------------
agent1       心跳中    最后 03:32:34  静默    0.1s  条目   1
agent2       心跳中    最后 03:32:34  静默    0.1s  条目   1
------------------------------------------------------------------------
⚠ 协作险情 1 条（心跳全绿也发现不了的那类）：
  [互相等待] <agent1> 与 <agent2> 正互相等对方回应（各卡在自己的 await 里）——谁都不会先答，这是一条真死锁。需要有一方先 brief 看到「待你回应 #N」并 reply，或由在线 agent 介入。
------------------------------------------------------------------------
用户喊话 0 条 · 未确认告警 1 条
  #1 [03:32:34] <agent1> 静默 0s 未确认
未闭环的提问 2 条：
  #1 <agent1> 问 <agent2>（3s，对方「心跳中」）：你的 schema 定了吗
  #2 <agent2> 问 <agent1>（3s，对方「心跳中」）：你的字段名定了吗

⚠ <watchdog> 03:32:34 [告警] [互相等待] <agent1> 与 <agent2> 正互相等对方回应（各卡在自己的 await 里）——谁都不会先答，这是一条真死锁。需要有一方先 brief 看到「待你回应 #N」并 reply，或由在线 agent 介入。

   ↑ 两个 agent 的心跳都是「心跳中、静默 0s」，一切正常 —— 但协作险情那行指出了真死锁。
     这正是「心跳全绿」类故障：没有进程崩溃、没有超时，就是谁也不动了。
     所以 check 在这里也必须是非零（1），否则这类故障在脚本里会被整个漏掉。

────────────────────────────────────────────────────────────
③ 通信熔断：两个 agent 互相客套停不下来
   同一对 agent 连续交替来回，到 6 轮开始提醒，到 12 轮直接拒绝写入。
   目的是把预算花在推进上，而不是「好的」「收到」「那就这样」的无限循环。

   第 13 轮 ask：
✗ 通信熔断：<agent1> 与 <agent2> 已经连续来回 12 轮，期间没有任何第三方或用户消息插入 —— 这几乎肯定是「互相确认」而不是推进，已经替你停下。
  收敛：post --agent agent1 --tag 决定 --text "结论是什么、接下来谁做什么"
  确实还要深挖：在刚才那条命令后加 --force（把当前这条命令原样重发并加 --force 即可）

   退出码 = 4（4 = 被机制叫停，不是参数错）。
   确认必须继续深挖时，加 --force 可以放行；但熔断想让你先写清结论。

────────────────────────────────────────────────────────────
④ 多路等待：一次等多人，谁先答都能往下走
   默认「全部都要」（--id 1,2）；加 --any 变成「任一即可」。

   bob 回了一条，然后用 --any 等：
<bob> 03:32:38 [回应#2] 本地文件就行，量不大
✓ #2 已由 <bob> 回应：本地文件就行，量不大
  等了 0s（--any：任一回应即达成）

   退出码 = 0（0 = 达成）。--any 拿到一条就返回，不会为了等齐所有人干耗。


────────────────────────────────────────────────────────────
  演示结束 —— 全部在 <临时目录>，已自动清理。
  真实用法：让每个 agent 每 15s 写一条心跳，另开一个 watch 当看门狗，
  具体命令见 README 的「30 秒上手」与 SKILL.md。

