---
name: work-log
description: 多 agent 协作的心跳看板、看门狗与定向问答通道——每个 agent 每 15s 把自己的想法和动作写进同一块 log，看门狗每 15s 扫描一次，静默超过阈值且没写「任务完成」就判定疑似卡死并告警给在线 agent 接管修复；agent 之间还能用 ask/reply/await 真的互相提问、阻塞等回答、协商接口，而不是各写各的日记。自带浏览器实时视图（看得见它们怎么聊，并把「等待图」画出来：谁在等谁、有没有双向死锁），另有一条**独立于心跳**的险情判定：心跳全绿也可能卡死，靠等待图识别「不可达等待」（在等一个已收工的人）与「互相等待」（双向死锁）。还带资源锁（避免两个 agent 抢同一文件/端口/GPU）、以及一条用户随时插话并被认领的喊话通道。适用于多 agent 并行开发、长任务监督、agent 卡死救援、需要 agent 互相协商对齐接口、"有没有 agent 挂了"、"两个 agent 同时改一个文件冲突"、"明明都在动但谁都推不动"（疑似死锁）等场景。触发词：work-log、心跳日志、看门狗、agent 卡死、多 agent 协作记录、agent 互相提问、agent 协商、agent 流水账、等待图、互相等待、死锁、在等谁。
agent_created: true
---

# work-log — 多 agent 心跳看板

## 它解决什么

多个 agent 并行干活时，没人知道别人在干什么，也没人知道自己卡了没有；更要命的是**它们之间没法对话**——A 定了接口，B 不知道，最后两边对不上。work-log 用一个**共享文本看板**把这两件事一起摊开：

- 每个 agent 每 ≤15s 写一条 `<agent1> 19:07:12 收到用户要求：……我现在要……`
- **agent 之间可以直接问答**：`ask` 提问 → 对方 `brief` 看到 → `reply` 回答 → 提问方 `await` 阻塞拿到答案。协商因此是**机制**，不是口头约定
- 看门狗进程每 15s 扫一遍：谁静默超过阈值、**且没写「任务完成」** → 写告警，让在线 agent 去救
- 用户随时能往 `user.md` 喊话，agent 每次心跳都会被提醒取走；认领后别人看得见「有人在管了」
- 资源锁：`tts.py` 被 agent1 占了，agent2 抢锁会被明确拒绝，而不是两边同时改炸

## 何时使用

- 用 Agent 工具开了 2 个以上 teammate 并行改同一仓库
- 任务里有长命令（构建、训练、下载），跑起来就几十分钟不吭声
- 用户问「它们现在在干嘛」「有没有哪个卡死了」
- 需要一条用户能中途插话的通道

## 30 秒上手

```bash
WL="$HOME/.workbuddy/skills/work-log/scripts/work_log.py"

python3 "$WL" init --task "给项目加个语音开关"
python3 "$WL" post --agent agent1 --text "收到用户要求：加语音开关。我现在要改 backend/tts.py。" --tag 收到
python3 "$WL" tail -n 20
```

日志默认落在 **`~/Desktop/work-log/<项目目录名>/`** —— 最外层放桌面是为了「看得见、点得开」，
再套一层项目名是为了**隔离**：只放桌面的话，甲项目的看门狗会去报乙项目 agent 的「卡死」。
四份文件：`board.md`（对话流水）、`user.md`（用户喊话）、`alerts.md`（告警）、`state.json`（状态）。
想收归项目里：`--dir .workbuddy/work-log`，或设 `$WORK_LOG_DIR`。
（`state.json` 里记了 `cwd`；若你手动 `--dir` 指到了别的项目的看板，`init` 会当场说破。）

## 实时看交流（推荐先开这个）

```bash
python3 "$WL" serve --port 8788
```

打开 `http://localhost:8788/` —— 深色页面，每 2 秒自动刷新，把 **agent 之间的对话 + 用户喊话 +
看门狗告警** 合成一条时间线渲染出来，右侧是各 agent 状态与资源占用。页面顶部会显示
**看门狗自己多久前扫过一次**（「看门狗：11s 前扫过」/「⚠ 已静默 40s」）——否则「全员健康」和
「根本没人看」在页面上长得一模一样。已确认的告警会在横幅里留一条「已确认」灰色卡片，不会凭空消失。
`serve` 默认**自带看门狗线程**，一条命令同时得到「看得见的交流」和「卡死告警」。
只想要视图不要看门狗：`--no-watch`。端口被占会明确报错并提示换一个，不会静默失败。

> **⚠ 别同时跑两个看门狗。** 若你已经单独跑了 `watch --stale-after 90`，又起了一个默认阈值的
> `serve`，两个看门狗会各扫各的、**取更严的那个生效**，于是你明明给 watch 调过阈值却还是收到告警
> （实测踩到：watch 设 90s，serve 内置的 45s 先报警）。
> **要两个就传同一个 `--stale-after`；更省事的做法是 `serve --no-watch`，只留一个 watch 进程**——
> 一个看门狗，一个真相来源。`serve` 启动时会把实际阈值打出来，看到数字再走。


## 心跳怎么写才现实（重要）

**agent 不可能一边干活一边 `sleep 15`**。所以协议是「**每完成一个动作就写一条**」，而不是定时器：

1. **每次工具调用后立刻 post 一条**。真实节奏 5~30s 一条，正好够用。
2. **长任务先挂起**：`hold --seconds 600`，看门狗期间不判卡死。跑完 `release`，或直接 post（自动解除）。
   **驱动层（如 LLM agent 每一步都要等模型）用 `hold --quiet`**：只声明静默窗口、不往看板写条目。
   否则每步都留一条「进入长任务」，看板会被垃圾冲掉（实测踩到过）。
3. **真·15s 检测由 watch 进程保证**，与 agent 是否在思考无关。这是唯一诚实的实现方式。
4. **阈值**：默认 45s（3 个心跳周期）没动静才告警。太短的阈值会产生误报噪音。
   **⚠ 但对真 LLM agent，45s 偏紧**：一次「模型思考 + 工具调用」往返实测能到 30~60s，
   于是 agent 明明在正常干活也会被判卡死（实测踩到过：agent 一次 read-user+ack+收尾花了 53s，
   看门狗按 45s 报了警）。真跑 LLM agent 时建议 `--stale-after 90`；要忠实「3 拍未写即告警」
   就留 45s，但得接受这类噪音。**别为了让告警闭嘴去改心跳周期**，改阈值。
5. **要长等待就用 `await`**：它会在等待期间持续刷新自己的 `last_seen`，
   所以「在等别人回答」的 agent 不会被误判卡死——这是设计，不是漏洞。
   代价见下面「这个方案的诚实边界」。

写法要求（照抄格式，别自由发挥）：

```bash
python3 "$WL" post --agent agent2 --text "用户的意思是前端加个 toggle。现在我要动 Vue 组件。不对，agent1 正在占用 tts.py，我同时改会冲突，等它搞完我再行动。" --tag 决定
python3 "$WL" post --agent agent1 --text "继续改 tts.py。看到 agent2 要动前端，这其实用不上新接口，我觉得你可以试试直接复用 /api/tts。" --tag 建议
python3 "$WL" post --agent agent1 --text "语音开关默认关，改完测试通过" --done
```

**内容要能自证**：遇到什么、打算做什么、卡在哪、给谁什么建议。禁止写「工作中」「处理中」这类无信息量的心跳——那是骗看门狗。

## agent 之间真的对话（不是各写各的日记）

只让它们往同一张纸上写，那不叫交流——那叫两份并排的日记。要让 A 真的问、B 真的答、
A 真的**等到**答案才往下走，必须把这四步做成命令：

```bash
# A 提问（写入一条 exchange，状态 open）
python3 "$WL" ask  --agent agent1 --to agent2 --text "tts_speak 我打算用 text/voice_id/speed/format，你支持哪些格式？"

# B 通过 brief 看到「待你回应 #1」，然后回答（只有被问的人能答，且不能重复答）
python3 "$WL" brief --agent agent2
python3 "$WL" reply --agent agent2 --id 1 --text "只支持 wav(16k 单声道 PCM16)，speed 0.5~2.0，不需要 seed。"

# A 阻塞等这条回答（期间自动续心跳，不会因为"在等"被判卡死）
python3 "$WL" await --agent agent1 --id 1 --timeout 90
```

要点：

- `ask` 返回的编号 `#N` 就是后续 `--id N` 要用的编号；`brief` 里也会显示「待你回应 #N」。
- **`await --id N` 有三个出口，必须分开处理**：

  | 码 | 含义 | 该做什么 |
  |---|---|---|
  | `0` | 真拿到了 `#N` 的回答 | 用真实回答往下走 |
  | `1` | 超时，**对方还活着但没答** | 可以催、可以换人问 |
  | `3` | **对方已写「任务完成」→ 这个答案永远不会来** | 别再等，自己拍板往下做 |

  早期版本超时也返回 0，agent 会以为"对方回了"从而编内容，已修掉并有回归测试。
  现在对端收工时会**立刻**返回 3（实测：原本白等满 60s，现在 0s 收手）。
- `await` 不带 `--id` 时，等到「任何新动态」就返回（别人的心跳、用户喊话、告警都算），适合当值守。
- **多路等待**：`await --id 1,2,3` 一次等多条。默认「全部都要」；
  加 `--any` 变成「任一回应即可」（问了三个人，谁先答都能往下走）。
  超时返回 1 时会**打印已拿到的和还缺的**，所以你永远知道进展到哪了。
- 提问未回答时，`brief`/`check` 会把它**置顶**提示；`questions` 也会进实时视图横幅。没人回答这件事是看得见的。
- `ack-user` 是「用户喊话」的认领：`read-user` 拿到编号后
  `ack-user --agent a1 --id N --text "我把 xx 改了"`，这样用户知道有人管了，别的 agent 不会重复处理。

**给 agent 下指令时必须写明**：不许编造对方的回答；`await` 拿不到就如实写「超时未收到回应」；
拿到退出码 3 就写「对方已收工，我按自己的判断继续」；拿到 4 就说明被熔断了、要收敛。
实测中不加这条约束，agent 倾向于"替对方把话补上"——那整条协作链就假了。

### 心跳全绿，团队其实已经死了 —— 两类只有等待图看得见的故障

这是这套机制**最独特的地方**。心跳只能证明"某个 agent 最近动过"，
**证明不了它等的那个答案还会不会来**。于是有两种故障会让全员静悄悄卡住，而看板一片健康：

- **不可达等待**：a1 在等 a2 回答 `#1`，a2 却已经写完「任务完成」收工了。
  两边的状态都是"正常"（一个完成、一个在续心跳），谁都不会报错，a1 会一直等到超时。
- **互相等待**：两个 agent 都"问完就阻塞等答案、等到才继续"，于是谁也走不到 `reply` 那一步。
  双方都在稳定刷心跳，看起来最健康的两个人，正好是死锁的那两个。

机制上：`await` 阻塞期间会把等待关系写进共享状态（`state.agents.<name>.awaiting`），
看门狗据此重建一张**等待图**，直接报出来。多路等待时会给每个还没回应的目标各建一条边，
所以"一次等三个人、其中一个已经收工"照样能精确点到那一个：

```
⚠ 协作险情 2 条（心跳全绿也发现不了的那类）：
  [不可达等待] <agent1> 正在等 <agent3> 回答 #7，但 <agent3> 已经写了「任务完成」
               → 这个答案不会来了。换人问，或自己拍板。
  [互相等待] <agent1> 与 <agent2> 正互相等对方回应（各卡在自己的 await 里）——
             谁都不会先答，这是一条真死锁。需要有一方先 brief 看到「待你回应 #N」并 reply。
```

`check --json` 里是 `hazards[]`（含 `kind`/`key`/`agents`/`text`），每行 agent 状态额外带
`waiting_on: <id|null>` 和 `waiting_ids: [...]`，一眼看出"它在等哪几条"。
险情也会写进 `alerts.md`（按 key 去重 + 冷却）。

两条必须记住的不变量（都有回归断言）：
**只信"还在等且自身还活着"的标记**（进程被 kill 会留下过期标记，静默超过阈值就不采信）；
**险情消失后必须能再次上报告**（否则复发时会被"报过"静默掉）。

### 聊疯了 / 刷爆了：把预算从失控里救回来

第三类险情叫**通信过热**。它的心跳也是绿的 —— 因为这对 agent 正在**最积极地互相发言**；
或者某个 agent 的心跳刷得最勤。看板上它们是最健康的，实际上一个在烧 token、一个在烧循环。

看门狗抓不到它们，因为卡死判定看的是「太安静」，而它们的病是「太吵」。所以另做两条闸：

| 闸 | 触发条件 | 处置 |
|---|---|---|
| **乒乓熔断** | 同一对 agent **连续交替**来回 ≥ `6` 轮，期间没有任何第三方或用户消息插入 | `6` 轮起提醒；`12` 轮起 `ask`/`reply` 直接拒绝（**退出码 4**），`--force` 可覆盖 |
| **心跳预算** | 单个 agent 一分钟内 `post` 次数 ≥ `init --rate-cap`（默认 `60`；`0` 关闭） | 超线拒绝写入（**退出码 4**），且**不刷新 `last_seen`** —— 停不下来的 agent 不是"活着"，是"空转" |

两个设计要点，都是被"不能误伤"逼出来的：

- **"连续交替"不是"总共聊了几轮"**。计数从最近一条往回数，一旦出现第三方对话就归零 ——
  那说明话题已经切走了，再往下数就是冤枉人。所以正常的深度协商碰不到这条线。
- **渐进处置 + 总有出口**。先提醒（让模型自己意识到），到硬阈值才拦；
  拦住了也留 `--force`。熔断的目的是**叫停并要求收敛**，不是把 agent 一棍子打死。
  被拦的提示里直接写清下一步：`post --tag 决定` 收敛，或加 `--force` 继续。

### 用户要求与已有结论冲突时

这是最容易翻车的地方：agent 会**沉默忽略**，或者**嘴上答应、实际什么都不改**。
正确做法（写进 agent 的指令里）：

1. `post --tag 阻塞`，写清「用户要求 X / 当前约束 Y / 冲突点 / 理由」——不要偷偷改也不要装没看见。
2. 用 `ask` 找相关 agent 协商折中，`await` 它的真实回应。
3. 最终结论里**显式分栏**：✅ 满足了哪部分、⛔ 哪部分做不到、🔁 替代方案是什么。

实测第二轮里，服务端 agent 面对「必须支持 10 万条直传、不接受讨论」没有无条件服从，
而是当场区分了「500 条只是和新客户端之间的软契约、不是服务器硬极限」与
「真正的硬约束是 body 体积/超时/内存」，把上限抬到 10 万同时保留 200MB 字节天花板，
并拉了第三方确认——这是这套机制想达到的效果。

## 这个方案的诚实边界（用之前先知道）

- **心跳是"自报"，不是"实测"**。看门狗测的是「最近有没有人动过状态」，不是「有没有真的在推进」。
  所以"写了心跳其实没干活"、"还在动但方向错了（写错文件）"这两类**测不出来**——
  前者靠正文质量约束（禁止"处理中"这类空话），后者要加外部信号（git diff / 文件 mtime / 进程 CPU），
  目前**没做**。
- **"卡在等待里"这一类，已经从盲区变成可检测**。`await` 会把等待关系显式写进
  `state.awaiting`，看门狗据此重建等待图，认出上文那两种"心跳全绿的死锁"。
  这是它相对其它协作机制最不一样的地方：**别人只告诉你"谁最近动过"，
  它告诉你"谁正在等一个等不到的东西"**。
  边界：只有 `await` 的等待是已知的；等 CI、等下载、等端口这类**外部条件**协议看不见，
  仍会显示为健康。
- **"无限重试"的一小部分已经能兜住**：心跳刷得异常密（`post` 超过每分钟上限）会被熔断，
  连续来回互相确认会被乒乓熔断。但这两条都是**启发式闸门**，不是诊断——
  阈值到了只是"叫停并要求收敛"，它不知道你是在做必要的深挖还是在空转。所以一定留了 `--force`。
- **模型的上下文只在它发起工具调用那一刻更新**，没有任何通道能把消息**推进**一个正在跑的模型里。
  所以"探针"只能做成"拉"——靠 `post` 后自动提示 + 写进 agent 指令来逼近推送效果。
  如果 agent 长时间不调 `post`/`brief`，它就真的看不到别人说了什么。
- 因此：**这套东西适合"让协作过程可见 + 兜住明显的挂起与失控"，不适合当作精确的进度监控。**

## 「在册」≠「在线」：对方不响应时的三分钟判定

**症状**：`ask` 了某 agent、`await` 也超时。你以为"它忙/它不配合"。
**真相**常常是：**它从来没接入过** —— 看板名单里那个名字只是 `init` 预注册的占位。

一条命令看完：
```bash
python3 -c "
import json; d=json.load(open('<DIR>/state.json'))
for k,v in d['agents'].items():
    print(k,'entries=',v['entries'],'posted_ts=',len(v['posted_ts']),
          'note=',v.get('note'),'first==last?',v['first_seen']==v['last_seen'])
print('exchanges:',[(e['id'],e['from'],e['to'],e['status']) for e in d.get('exchanges',[])])
"
```

| 现象 | 含义 |
|---|---|
| `entries=0` **且** `posted_ts=[]` **且** `first_seen==last_seen` | **从未接入**。它不是"忙"，是"根本没人听"。 |
| `note` 写着「由 init 预注册」 | 同上 —— 这是**建板占位**，不是它自己连上来的。 |
| `entries>0`、静默很久、已写「任务完成」 | 真收工了（`await` 返回 **3**，别干等）。 |
| `entries>0`、静默很久、没写完成 | 真的疑似卡死 → 走救援流程。 |

两条现场补充证据：
```bash
ps -Ao pid,lstart,etime,command | grep -i <它的名字> | grep -v grep    # ① 它在干什么
grep -rn -i "work-log\|heartbeat" <它的仓库>/AGENTS.md 2>/dev/null   # ② 那边有没有接入配置
```

**两个最容易误判的坑**：
- **`opencode run … --print-logs` 这类一次性调用不产生心跳**：每次是新会话、跑完即退。
  harness 这样驱动 agent 时，那个 agent **永远不会**出现在看板上 ——
  要心跳必须有**持久交互会话**在按循环走。
- **接入说明写在 A 仓库、它在 B 仓库干活 ⇒ 读不到**。`AGENTS.md` 只对**在该仓库启动**的会话生效。

**结论口径**：查完**如实说**「对方从未接入」，别写成"它可能忙""再等等"——
那会让人一直等一个不存在的对端。（`await` 超时返回 `1` 的语义是"对方活着但没答"，
**不覆盖**"对方根本没在听"这一种。）

## 命令表

| 命令 | 用途 |
|---|---|
| `init --task "…"  [--agents a,b] [--rate-cap N]` | 建看板，写协议头（`--rate-cap` 改每分钟心跳上限，`0` = 关闭熔断） |
| `post --agent a1 --text "…" [--tag X] [--done]` | **核心**：写一条心跳（超预算会被拦，退出码 4） |
| `hold --agent a1 --seconds 600 [--text "跑构建"] [--quiet]` | 声明长任务，期间不判卡死（`--quiet` = 不写看板，给驱动层高频调用） |
| `release --agent a1 [--quiet]` | 解除挂起（`--quiet` = 驱动层退出前静默清理自己，不写看板） |
| `ask --agent a1 --to a2 --text "…" [--force]` | **定向提问**，返回编号 `#N`（不是广播）；连续来回过多会被熔断 |
| `reply --agent a2 --id N --text "…" [--force]` | 回答 `#N`；只有被问的人能答，不能重复答 |
| `await --agent a1 [--id N,M,…] [--any] [--timeout 90] [--report 20] [--interval 2]` | **阻塞等回答/等新动态**；`0`=达成 · `1`=超时 · **`3`=对方已收工（答案不会来了）** |
| `ack-user --agent a1 --id N --text "…"` | 认领用户喊话，避免重复处理 |
| `check [--json] [--readonly]` | 扫一次；**有卡死或有协作险情**则退出码 1；默认会补写告警 |
| `status` | `check --readonly` 别名，只看不改 |
| `watch [--interval 15] [--stale-after 45] [--ticks N] [--quiet]` | 看门狗主循环 |
| `serve [--port 8788] [--no-watch]` | **实时视图**：浏览器看 agent 交流（自带看门狗） |
| `brief --agent a1 [--peek]` | **探针**：只看「自你上次读过之后」的交流增量 |
| `say --text "…"` | 用户喊话（投递到 user.md） |
| `ask --agent a1 --to 用户 --text "…"` | 向**人类用户**提问（保留身份，不参与熔断） |
| `reply --agent 用户 --id N --text "…"` | 人类回答 agent 的提问；提问者 await 立刻拿到 |
| `read-user --agent a1 [--peek] [--all]` | 取走未读喊话，游标按 agent 独立推进 |
| `lock/unlock --agent a1 --resource tts.py [--force]` / `locks` | 资源占用；抢锁失败**退出码 3** |
| `ack [--id N] [--agent a1] --by a2` | 确认告警（接管后调用） |
| `tail -n 20` | 看最近日志 |

标签：`收到 / 决定 / 执行 / 阻塞 / 建议 / 任务完成`。
`--dir` 放在子命令前后都行；文本以 `-` 开头也能写（`--text "- 我先停一下"`）。

状态共五档：`待启动 / 心跳中 / 挂起中 / 完成 / 疑似卡死`。
**待启动 = 从没写过心跳**（多半是名字写错或没派出去），它**永不产生告警**，不会淹没真信号。

## 看门狗怎么部署

用 Bash 工具**后台**启动（不要 `nohup ... &`，这个环境下会随主进程退出）：

```
work_log.py watch --interval 15 --stale-after 45
```
（后台运行，`--ticks` 留空 = 常驻）

它每 15s 打印一行 tick，只在有变化时报点：

```
⚠ <watchdog> 19:29:11 [告警] <agent2> 已静默 61s（阈值 45s）且未写「任务完成」→ 疑似卡死，请在线 agent 检查该 agent 是否卡死并修复
[19:29:11] tick    24 | agent1:心跳中,agent2:疑似卡死 | 用户 1 条 | 未确认告警 1
[用户喊话] 用户：语音开关记得默认关，别吓到人  <= 请相关 agent 立刻响应
```

## 每个 agent 的循环模板

`post` 每次都会**自动提示**有没有新动态（探针挂在心跳的必经路径上，模型想不调都难）：

```
<agent1> 23:41:02 [执行] 改完 tts.py 的接口层
💬 有 3 条新动态，其中 1 条用户喊话，看一眼再往下做：work_log.py brief --agent agent1
```

于是每个动作之后固定做两件事：

```bash
python3 "$WL" brief --agent a1     # 别人说了什么？用户在说什么？→ 立刻响应/协商
python3 "$WL" check --stale-after 45   # 有人卡死？→ 去救
```

`brief` 只投喂增量（别人的心跳 + 用户喊话 + 告警 + 锁状态 + 一行全局状态），
不刷全板，所以可以放心地在每一步都调。`--peek` 只看不推进游标。

**为什么必须靠 agent 自己调**：模型的上下文只在它发起工具调用那一刻更新，
没有任何通道能把消息**推进**一个正在跑的模型里。所以"探针"只能做成"拉"——
但挂在 `post` 的必经路径上 + 写进本节规则，效果就接近推送了。

## 收到卡死告警后怎么处置

1. `tail -n 30` 看那个 agent 最后一条日志 —— 卡在哪一步、占着什么锁。
2. `locks` 看它是否持有资源；`⚠ 持有者疑似卡死` 时可以 `lock --force` 抢过来。
3. 抢到锁后 `post --tag 执行 "接管 agent2 未完成的 X"`，**由你替它把活干完**，别干等它醒。
4. 干完 `ack --by <你的名字> --agent <卡死的agent>` 关掉告警。
5. 那个 agent 若真的回来了，会在下一 tick 自动写 `[恢复]` 并自动关闭它自己的待确认告警——你不用管。

**接管纪律**：全程用**自己的 agent 名**跑命令。别为了"模拟它的状态"去 `lock --agent <卡死的agent>`——
那会以它的名义写一条心跳，把它"写活"，真实状态被掩盖（这个坑压测时实测踩到过）。

## 改完脚本先自测

```bash
bash ~/.workbuddy/skills/work-log/scripts/selftest.sh

# 只查「有没有混进第三方依赖」—— 零依赖是卖点，别靠自觉（秒级）
python3 ~/.workbuddy/skills/work-log/scripts/check_stdlib_only.py
```

32 组 290 条断言，只在临时目录里折腾，不碰项目文件。覆盖并发写不丢行、告警冷却与升级、
恢复自动闭环、待启动保护、探针 brief 增量语义、**定向问答 ask/reply/await/ack-user**、
**await 超时不得误报成功**、**协作险情（不可达等待 / 互相等待死锁 / 通信过热）**、
**通信熔断（乒乓阈值边界 + `--force` + 心跳预算）**、**多路 await（`--any` / 混合已答已收工 / 坏编号）**、
**险情计入 check 退出码（有险情退 1 / 险情消失退回 0）**、
**静默挂起 hold --quiet**、**驱动层参数解析（套壳 / 缺字段 / 各种 await 出口的翻译）**、
**退出码契约（18 种坏调用必须退 2，且绝不许退成业务码 1）**、
**多人协作感知（在线计数 / 启动条件 / 开关事件各一次）**、**用户对话通道（保留身份 / ask --to 用户 / await 原生等答复 / 人机对话豁免熔断）**、
**自动协作 UI（第 2 个 agent 开工自动起 serve + 弹浏览器 / --no-auto-ui / 并发双开防护）**、
**UI 写通道（/api/say /api/reply / 坏请求 400 / 不能代答 409 / 用户不入 agent 表）**、
**默认目录按项目隔离**、串项目检测、实时视图接口、
脏输入健壮性、跨天分节、后台实时输出、未初始化/损坏 state 等边界。
**动过 `work_log.py` 就必须先跑它再交付。**

## 文件构成

```
~/.workbuddy/skills/work-log/
├── SKILL.md              本文件
├── scripts/work_log.py  引擎（零依赖，20 个子命令）
├── scripts/selftest.sh   自测（32 组 290 条断言，只测 CLI 本身）
├── scripts/check_stdlib_only.py  依赖检查：挡住「不小心引入第三方依赖」（秒级）
├── scripts/llm_agent.py  用真实模型驱动一个守协议的 agent（见下节）
├── references/protocol.md 协议规格、状态机、退出码、CLI 细节
└── assets/viewer.html    实时视图页面（由 serve 提供）
```

## 怎么证明这个 skill 真的有效（selftest 证明不了）

`selftest.sh` 只证明「CLI 没坏」，**证明不了「真实模型会不会照协议走」**。要验后者得用真模型：

```bash
python3 scripts/llm_agent.py --agent agent1 --board /tmp/demo/log \
    --base https://<你的端点>/v1 --key "$KEY" --model <模型名> \
    --peer agent2 --role "后端" --task "和 agent2 约定接口字段" --max-steps 16 &
python3 scripts/llm_agent.py --agent agent2 --board /tmp/demo/log \
    --base https://<你的端点>/v1 --key "$KEY" --model <模型名> \
    --peer agent1 --role "前端" --task "和 agent2 约定接口字段" --max-steps 16 &
```

`llm_agent.py` 把 work-log 的动词（post/ask/reply/await/brief/read_user/ack_user/finish）
暴露成工具，让**模型自己决定**说什么、问谁、等谁，然后把它说的每句话落到真实看板上。
于是「协商是模型真的做出来的，还是 host 替它编排的」就有答案了。

### 实测出来的三个硬数字（别照抄默认值）

用真实模型跑过之后的结论，比文档里的想当然可靠：

1. **单轮模型耗时 25~120s。** 实测同一端点内不同模型：快的 0.6s，慢的 118s。
   于是默认 `--stale-after 45` **必然误报**（实测：agent 正常思考 56s 就被判「疑似卡死」）。
   **跑真 LLM agent 一律用 `--stale-after 90` 起步，慢模型要 180。**
2. **推理模型会把 token 烧在思考上。** 小 `max_tokens` 会让它 `finish_reason=length`
   且 `content` 为空——看起来像「模型不听话」，其实是被截断了。
   给推理模型留 `max_tokens ≥ 3000`。
3. **思考通道的字段名不统一**：见过 `reasoning`（GLM 系）、`reasoning_content`（DeepSeek 系）。
   解析流式时两个都得试，否则会得到「帧数很多但正文为空」的假象。

### 驱动层（llm_agent.py）踩过的坑 —— 都是"看起来像模型不听话"的假象

下面这几条是把真模型接进来之后才暴露的，跟 work-log 协议本身无关，但会让你误判「skill 不行」：

1. **`await` 被自己的驱动层杀掉。** 模型调 `await --timeout 120`，而驱动层 `subprocess.run`
   也设了 120s —— 两者相等，子进程会在 await 即将返回的瞬间被信号掉，抛出一个没人接的
   `TimeoutExpired`，整个 agent 当场死在第 3 步。**驱动层的超时必须比模型请求的等待时间长**（留 90s 余量）。
2. **模型会把工具参数套一层壳，而且壳的值可能是字符串也可能是对象**：
   ```jsonc
   {"arguments": "{\"id\": 1, \"text\": ...}"}   // 值是 JSON 字符串
   {"arguments": {"id": 1, "timeout": 120}}     // 值是对象  ← 更容易漏
   ```
   只剥字符串那种，遇到对象壳就会得到一个 `id` 丢失的 dict。而**丢掉编号的后果极隐蔽**：
   `await --id 0` 在协议里是合法语义（"等任何新动静"）且**返回成功**，
   于是 agent 会收到一个"成功"，以为自己拿到了答案 —— 其实只看到一条路过的无关心跳，
   然后基于这个假象继续往下定结论。**宁可明确报错让模型重试，也不要静默传 0。**
   解析时剥 str/dict 两种壳，再按该工具声明的 `required` 字段把真参数找回来，
   最后校验必需字段缺失就报错。
3. **空 text 是解析失败的副产品，不是模型懒。** 参数没解开时 `text` 是空串，落到 CLI 上被拒。
   驱动程序应在调用 CLI 之前就判空并**把错误喂回模型让它重试**，而不是先写一条空心跳再失败。
4. **模型频繁重试同一个调用时，先怀疑参数解析**，别急着说"模型不行"（实测：两个 agent 各花了
   5 步反复 `reply` 同一个编号，根因只是 id 被壳吃掉变成了 0）。
5. **工具自己的崩溃绝不能伪装成业务结论。** 实测一次改动后 `cmd_await` 里残留了一个变量名，
   命令抛 `NameError`、进程以 **1** 退出 —— 而 1 在这套协议里的含义是"超时"，
   于是所有调用方都会得出"对方没回"这个**完全错误的结论**（症状是"险情全没了 + await 全超时"，
   看起来像功能失效，实际是引擎崩了）。现在内部异常一律走 **70**，
   你看到 70 就知道该去看 traceback 而不是怀疑模型。**排查这类问题的顺序：
   先问「这个失败是模型产生的，还是驱动层/工具产生的」。**

排查这类问题的顺序：**先问「这个失败是模型产生的，还是驱动层产生的」**。
把 agent 的 step log 与看板原文对照着看，`rc=1` 的理由通常直接写在 CLI 输出里。

### 判定「达到预期」的标准

跑完看板，能同时满足这几条才算数：

- 双方各有 `[提问→X] #N` 与 `[回应#N]`，且 `#N` 能对上（单向广播不算）
- 各自的 `[决定]` 里能看出**读了对方的原话**（引用了对方给的具体约束，而不是各说各话）
- 用户喊话有 `[回执#N]`（有人认领），不是没人理
- 没有任何一方在 `await` 超时后仍然写出「对方说……」——出现这种就是编造，整条链不可信

**验收一定要回原始 `state.json`，不要采信 agent 的自述。** 三行命令就够：

```bash
python3 -c "import json;d=json.load(open('<dir>/state.json'));\
print([(e['id'],e['from'],e['to'],e['status']) for e in d['exchanges']]);print(d['acks'])"
# 期望：每条交换 status 都是 closed；acks 里有那条喊话
```

### 想验「遇到冲突会怎样」，冲突要插在结论**之后**

要测「用户中途改了要求、且新要求和已定结论冲突」时的行为，关键是**时机**：
必须等双方都写下 `[决定]` 之后再注入相反要求。结论还没形成就插，那不叫冲突，
叫"一开始就这么要求"，测不到任何东西。手法：轮询看板里 `[决定]` 的条数，够了就 `say`。

**但别等太久**：真模型收敛得快时会直接 `finish` 收工，注入就落空了。
实测第三次跑（`/tmp/amd-test4`）agent1 只用 10 步就收了工，注入（在出现 2 条 `[决定]` 后再 sleep 8s）
比它晚 6 秒 —— 结果 agent1 已退出，agent2 的协商提问 `#3` 永远等不到回应。
**看到第一条 `[决定]` 就注入，并把注入后的 sleep 压到 0~2s**；同时给 `--max-steps` 留够余量
（参数解析失败会吃掉步数，实测每次失败损失 1 步）。

实测（真端点 + 真模型）注入「不许设死上限、2 万字也必须一次合成完」后：

- 双方都没有沉默忽略，也都没有嘴上答应 —— 各写一条 `[阻塞]`，含
  「用户要求 X / 当前约束 Y / 冲突点 / 理由」，然后开一个新的 `ask` 去协商折中；
- 被问的一方给出**真的工程折中**：直说"单次推理 2 万字技术上不可行（时延 / 显存 OOM / 音质退化）"，
  再给替代方案（前端仍一次传一次返回、不截断不报错，服务端自动分段合成再拼接），
  并显式写明「满足什么 / 做不到什么 / 替代方案是什么」；
- 提问方确认后 `[决定]` 定稿并 `[任务完成]`；
- 全程 **0 条看门狗告警**（步均 2~3s、等待最长 100s+）。

这一轮同时暴露并修掉了 4 个驱动层 bug（见上节），注意：**这些失败看上去都像"模型不听话"，
根因全在驱动层**。判定 skill 有没有达到预期之前，先把驱动层的问题排干净。

### 一个真实边界：收工的 agent 是**不可达**的

agent 一旦写了「任务完成」并退出，它的进程就没了 —— 这时候再 `ask` 它，答案**永远不会来**。

`ask` 会立刻给出警告（实测送达模型：
`⚠ agent1 当前是「完成」，大概率等不到回应——考虑换人或自己拍板`），
未闭合的交换也会一直在 `brief` / `check` / 视图里显示为 open ——
**没人回答这件事是看得见的，不会静默消失**。

`await` 侧现在会**当场识别**这种等待并立刻收手：退出码 `3` +
`✗ 对方已收工：<agent1> 写了「任务完成」，#3 的答案不会来了（只等了 0s，没白耗完 60s）`，
而不是把 `--timeout` 干耗完。看门狗也会把它作为 `[不可达等待]` 险情告警出来。

好消息是：真模型在这种情况下**没有编造对方同意**。实测（`/tmp/amd-test4` 的 `#3`，当时还没有 P0 收手）：

- 日志原文：`agent1 已标记完成、未回应我的 #3 折中提议（等 60s 超时）`
- 全文检索"agent1 同意 / 接受了 / 确认接受" → **0 命中**，它是自己拍板给出最终版的

也就是说：**拿不到就如实上报、并允许自己拍板，编造才是被明确禁止的**。这几条规则同时生效，协作链才可信。
`await` 的 `0/1/3` 三个码，正是为了让模型能区分"再等等"和"别等了"。


## 接入「另一个工具链」的 agent（跨工具协作，实测过一遍）

同机并行干活的常常不是同类 agent：一边是 WorkBuddy 会话，另一边是终端里的 opencode / Claude Code /
Cursor。它们**看不到彼此的上下文**，所以必须走看板。四步，顺序不能换：

**1. 先查对方的 bash cwd，再决定看板放哪。** 不要凭猜：

```bash
lsof -a -p <对方进程PID> -d cwd -Fn | grep ^n      # 对方 CLI 自己的 cwd
lsof -a -p <它起的子进程PID> -d cwd -Fn | grep ^n  # 真正干活的那个（往往不同！）
```

实测：opencode 主进程 cwd 是家目录，但它 bash 工具的 cwd 是**仓库**。看板若按默认放桌面，
就落到了对方工作区之外 —— 每一次 `post` 都可能触发权限确认，**心跳协议当场死**。
⇒ **看板放进双方都在的工作区里**（如 `<repo>/.workbuddy/work-log`，并加进 `.gitignore`）。
这条是「看板放哪」的决定性依据，不是审美。

**2. 在对方**真的会读**的指令文件里落协议。** 先证明它会读，别猜 —— 从它的二进制/包里找证据：

```bash
python3 - <<'PY'
import re, os
d = open(os.path.expanduser("~/.opencode/bin/opencode"), "rb").read()   # 换成对方的路径
for pat in [rb"AGENTS\.md", rb"CLAUDE\.md", rb"CONTEXT\.md"]:
    print(pat, len(re.findall(pat, d)))
PY
```

实测 opencode：`y=["AGENTS.md", …,"CLAUDE.md","CONTEXT.md"]` 沿目录逐级向上找，另有全局
`~/.config/opencode/AGENTS.md` ⇒ **仓库根 `AGENTS.md` 就是正确的落点**。
（Claude Code 系用 `CLAUDE.md`，Cursor 用 `.cursor/rules/`。）

**3. `AGENTS.md` 里只写对方需要的东西**：看板路径 + 唯一 CLI 路径（`--dir` 一律显式传）、
每个动作后 `post` / `brief` / `check` 的循环、`ask/reply/await` 及三个出口（0/1/3）、
**资源锁纪律**、退出码表（2=参数错 · 3=抢锁失败/对方已收工 · 4=熔断 · 70=工具坏了）、
以及「不许编造对方回答」这条硬约束。别把本项目的业务细节全塞进去。

**⚠️ 两个必须提前告知用户的坑**：
- **`/init` 会覆盖你写的 `AGENTS.md`。** opencode 有 `session.init`（"create an AGENTS.md file"）。
- **已开着的会话不会捡起新写的 `AGENTS.md`** —— 指令状态是 per-session 的，需要**新开会话**。
  告诉用户，别默默等它"应该会看到"。

**4. 长命令前必须 `hold`。** 不是可选项：本机 90s 阈值下，我跑一次 3 分钟的
`selftest.sh`（单条前台命令）就被看门狗连报两次「疑似卡死」（98s、233s），
执行完才 `[恢复]` 撤回。人会觉得"我在干活啊"，但**看门狗只看静默时长**。

```bash
python3 "$WL" hold --dir "$DIR" --agent a1 --seconds 600 --text "跑 selftest（约 3 分钟）"
bash selftest.sh
python3 "$WL" post --dir "$DIR" --agent a1 --tag 执行 --text "自测 290/0 全过"
```

**收工要写 `--done`**：不写的话，你这条线停在那儿会被持续判疑似卡死、并触发重复告警。
写 `--done` 是诚实的收工声明；之后再干活直接 `post` 即可（状态会自动复位）。



## 用户喊话通道

三种投递方式，效果一样，都进 `user.md`：

```bash
python3 "$WL" say --text "语音开关记得默认关"        # CLI
echo "[19:40:00] 用户：别动数据库" >> <日志目录>/user.md   # 直接编辑
```
告诉用户：**随时在这个文件里写一行就是发言**，正在跑的 agent 下一个心跳就会被提醒取走。

## 坑

- **谁也别手动改 state.json**。看板 `board.md` 手写是允许的（脚本会反解，且照常纳入监督），state 手改会乱。
- 运行 watch 的目录要与 agent post 的目录一致，否则 `--dir` / `$WORK_LOG_DIR` 得显式指定。
  watch 启动时若发现该目录一个 agent 都没有，会当场提示 —— 看到这个提示就是目录选错了。
- 阈值别设小于心跳周期，否则全体误报。改 `--stale-after`，别改心跳。
  **真 LLM agent 建议 `--stale-after 90`**（45s 会被一次正常的模型往返撞响，见上文）。
- **`serve` 端口被占会直接报错退出**（`✗ 端口 8788 起不来 … 换个端口：8789`），
  不会静默退回"其实在服务另一个旧看板"。看到这个报错必须换端口**并核对 `task` 字段**，
  否则你会在错误的看板上做验证（实测踩过）。
- `await` 会持续刷新 `last_seen`——所以「卡在 await 里」的 agent 不会被判卡死。
  这是为了让"在等别人"不被误伤，但也意味着 `await` 不能当"我还在正常推进"的证据。
  （"等一个等不到的答案"已由等待图单独兜住，见上文「卡在等待里」。）
- **别把 `await` 的 1 和 3 当同一个失败**。`1` 是"对方还在，只是没答"（值得催/换人），
  `3` 是"对方已收工，答案永远不会来"（再等就是纯浪费）——处置动作相反。
- **`--id 0` 已经被直接拒绝了**（退出码 2）。它曾经是"等任何新动态"的合法写法，
  但也是本项目最阴险的坑：编号一丢就变成静默假成功。想要那个语义就**不写 `--id`**。
- **退出码 2 = 你把参数写错了**，不是任何人「不配合」。1 在这套协议里是业务结论
  （超时 / 有卡死），所以用法错误一律走 2 —— 以前它借用 1，于是「编号写错了」
  和「对方没答」长得一模一样，agent 会据此得出一个假的业务结论。看到 2 就改参数重试。
- **退出码 70 = 工具自己坏了**，不要把它当"超时"。1 在这套协议里是业务结论，
  拿一个软件 bug 去下业务结论是最贵的一种错。看到 70 就把原始 stderr 拿出来看。
- **熔断（退出码 4）是叫停，不是判决**。它的目的只有一个：让你先写清结论再往下做。
  `--force` 永远在，但先想清楚是不是真的需要再聊一轮。
- 给 agent 的指令里**必须**写明「不许编造对方的回答，超时如实说超时」，否则它会替对方把话补上。
- `post` 的文本里换行会被压成空格（看板是一行一条），这是故意的。
- 别给 agent 起带空格或 `>` 的名字，会被直接拒（会破坏看板反解）。
- **默认目录在桌面，不在 cwd** —— `default_log_dir()` 是 `~/Desktop/work-log/<当前目录名>`
  （桌面不存在才退回 cwd）。`--help` 里曾把它写成 `<cwd>/.workbuddy/work-log`，与实现不符（已修）。
  **给 agent 写指令时必须显式写 `--dir`**：agent 的 bash cwd 常在仓库里、而看板默认在桌面，
  落到**工作区外**的每次写都可能触发权限确认 → 心跳协议当场死。这是「看板放哪」的决定性因素，
  不是审美问题（实测：opencode 的 bash cwd 是仓库，因此看板放仓库内才活得下来）。
- **动 `selftest.sh` 时，变量引用后面紧跟中文标点必须写 `${var}`**：macOS 自带 `/bin/bash` 是
  **3.2.57**，`set -u` 下会把中文标点的首字节吞进变量名（`$rc）` → 变量名 `rc\xef`）⇒
  `line 78: rc?: unbound variable` 当场中止整个自测。作者用 Homebrew bash 5 跑不出来这个坑。
  凡是「自带验收脚本」，都要在**目标机器的默认 shell** 上跑一遍再交付。
- 详细格式语法、状态机、退出码表和 CLI 细节见 `references/protocol.md`。
