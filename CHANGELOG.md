# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
日期为开发日期；`0.x` 期间接口仍可能微调，但**退出码契约已冻结**（见下）。

---

## [0.7.0] — 2026-09-25 · 固定 agent 名字 + 换台电脑也能用

主题：`0.6.0` 把「接入」补齐了两端（`onboard` 交付、`doctor` 自查），但**接进来之后**还有两类
失效会在**没有任何人报错**的情况下发生 —— 而它们恰好都发生在"看起来一切正常"的时候：

- **名字撞了。** `_get_agent(st, name, t)` 按名字取的是 `st["agents"][name]` —— **同一个 dict**。
  两个会话叫同一个名字时，它们共享 `last_seen` / `entries` / `board_cursor` / `user_cursor` /
  `awaiting`。后果不是"名字不好看"：看门狗看到的是**两个人交替刷新**，于是**谁都不会被判卡死**；
  `entries` 是两个人的合计，「待启动 / 从未写过心跳」判据失效；`awaiting` 被后写的人覆盖，
  等待图画出错的边。**而板上完全看不出来。** 这是整套判断（谁卡死、谁在等谁）唯一的地基。
- **换了机器。** 接入片段是要**离开这台机器**的（抄进对方的记忆文件、拷到别的电脑）。
  它以前写死 `工具路径：/Users/<某人>/…/work_log.py` —— 到了第二台机器上就是一条**死链**，
  而且失效是静默的：对方照抄、报个"文件不存在"，然后**不再报到**。
  也就是 `0.6.0` 主题里那次事故的复现路径。

顺带把「落点」这一步从**人工**改成**观测**（`onboard --detect` / `--to auto`）：
上一版说「落点只能靠看对方二进制的证据」—— 那句话对，但它把「要靠证据」
错说成了「要靠人去查」。装过的工具链一定留下痕迹，所以这是可观测的。

### Added

- **`identity` 子命令（`list` / `set` / `rm`）+ 看板里的 `identities.json`。**
  形状 `{"<名字>": {"cwd", "kind", "purpose", "since"}}`，**跟着看板走**而不是跟着机器走。
  核心语义是**让抢名字变成一件会被拒绝的事**：别的 `cwd` 已经占着这个名字时
  `identity set` **退 2**，并把占用者的目录打出来。`--force` / `$WORK_LOG_FORCE_IDENTITY=1`
  是"你确实换了目录"的逃生口（它的语义是**把这条登记改到我现在这个目录**）。
  同一 `cwd` 可以登记多个名字（真的一个目录里跑两个会话），此时任何自动解析都会因歧义退 2 ——
  那两个会话必须各自显式传 `--agent`。
- **`whoami` 子命令。** 把「我在这块板上叫什么、**凭什么**」打出来。
  身份固定这件事如果不可查验，就只是又一句口头约定 —— 这是加它的唯一理由。
- **`--agent` 从必填改成可省（11 处），并新增三档解析 `resolve_agent()`。**
  从**最明确**到**最自动**：① 命令行 `--agent` → ② `$WORK_LOG_AGENT`（给**会话**钉死）
  → ③ 板上 `identities.json` 里 `cwd == 我当前目录` 的那条（给**目录**钉死）。
  三档都不成立就**退 2**并打印可抄的命令 —— **绝不猜**。
  - 刻意**没有**「板上只有一个名字就用它」这条规则：那会在第二个 agent 还没登记的窗口期
    把它静默塞进第一个人的身份里 —— 正好是最该拦住的时候。
  - `doctor` / `whoami` 用非致命的 `_try_resolve()`：诊断命令的职责是把现状说出来，
    不是"因为现状不合规就当场退出"。
- **`init` 在看板目录里落一个可执行的 `worklog` 引导脚本**（`write_bootstrap()`，由
  `ensure_files()` 调用 ⇒ **任何一次 `post` 都会自愈补齐**旧板）。它干三件事：
  钉死 `--dir`、**自己找引擎**、设 `$WORK_LOG_SELF`。
  查找顺序：`$WORK_LOG_BIN` → `<看板>/.worklogrc` 的 `WORK_LOG_ENGINE`
  → `<看板>/work_log.py` → `<看板>/scripts/` → 用户级安装位 → `$PATH`；
  全找不到就**退 70 并打印三条修法**，而不是抛一句语焉不详的报错。
  同时落 `<看板>/.worklogrc`（本机私有：引擎位置 + 注释掉的默认身份）与
  只含它一行的 `<看板>/.gitignore`。
  - `.worklogrc` 写成 `export WORK_LOG_ENGINE="${WORK_LOG_ENGINE:-<本机路径>}"`——
    这个 `${VAR:-…}` 是刻意的：**环境里显式给的值优先**。否则会出现
    「我明明 export 了新的引擎位置，它却还在跑 rc 里那个旧的」，属于最难查的一类。
- **`onboard --to` 的落点别名从 3 个扩到 7 个**（按「换个目录开会话还认不认」分两档）：
  `user` / `opencode-global` / `claude-global` 是**跨目录**的，`repo` / `claude` / `gemini` /
  `cursor` 只对在该目录启动的会话生效。**别名只是别名，不是白名单** —— `--to <路径>` 一直可用。
  加这张表是因为「接入任何 agent」的难点就在它上面：不同工具链读的启动文件根本不是同一个，
  而这些差异没人记得住（当年的真实事故就是写进了只对某仓库生效的 `AGENTS.md`）。
- **`doctor ⑥ 名字固定`。** 它抓的是**另一个维度**的失效：③ 保证「我找得到这块板」，
  ⑥ 保证「我在这块板上有**专属**身份」—— 两者可以各自独立地成立。判据刻意**只把
  「连自己叫什么都定不下来」算病**：显式传 `--agent` 是能当场工作的，只是换会话要重来；
  那是提醒，不是故障（与 ④ 的静默判定同一套口径：**只认"现在能不能跑通"**）。
- **`self_cmd()`。** 引擎打印的所有「照抄这一行」提示改用 `$WORK_LOG_SELF`
  （由引导脚本设），于是提示里出现的是**看板自带的那个脚本**而不是引擎在本机的位置 ——
  否则换台电脑后，连错误提示里的命令都是死链。
- **`onboard --detect` 与 `onboard --to auto`：把落点判断从人工取证变成一次观测。**
  上一版说「落点只能靠看对方二进制的证据」—— 那句话对，但它把「要靠证据」错说成了
  「要靠人去查」。装过的工具链一定在机器上留下痕迹，所以这件事是**可观测的**：
  配置目录是否存在（`~/.config/opencode` / `~/.claude` / `~/.cursor` / `~/.gemini` …），
  或可执行文件是否在 `$PATH` 上。`--detect` 只探测、**一个字节都不写**（换台机器先跑它）；
  `--to auto` 按观测结果逐个落盘。每条都打出**命中的路径**当证据 ——
  「我探测到了」如果不可查验，就只是把口头约定换了个地方。
  三条口径写死在实现里：① **只落跨目录档**（`opencode-global` / `claude-global`），
  于是「对方在哪个目录启动」这个探测不出来的事实被整个绕开；② **`user` 默认跳过**
  （`--include-user` 才落），因为它每会话、每工作区都注入 —— 一块只服务某个项目的板
  把坐标写进用户级记忆，等于把 A 项目的板告诉 B 项目的会话；③ **探测到却落不了的必须说出来**：
  Cursor / Gemini CLI 只有仓库级落点，`auto` 会列出来并给替代命令，**不静默忽略**。
  一个可落的都没探测到时退 **`1`**（业务：什么都没达成），**不是 `2`** ——
  「这台机器上没装能自动落的工具链」不是用法错误。
- **批量落盘先预检（`_onboard_target(..., dry=True)`）。**
  `--to auto` 会在**一个字节都不写**之前把所有落点验一遍，任何一个会被拒就整体不落。
  半落状态比全不落难收拾得多 —— 没人知道哪几处生效了。
  判据（含"目标里已有手写协议"那条）与单点写入共用**同一处实现**，不分成两份。
- **自测第 `[37]` 组**（24 条，套件 36 组 347 条 → **37 组 371 条**）：沙箱 `HOME` **加**
  收窄 `PATH` 造出「装了」与「没装」两种机器各验一遍。**只改 `HOME` 不够** ——
  `$PATH` 上真实存在的 `opencode` / `claude` 会被照常命中，于是"空机器"根本模拟不出来
  （第一版就栽在这儿：测出来的空机器其实是本机）。杀手锏断言是预检那条：
  一处被拒 ⇒ **另一处一个字节都没写**。
- **自测第 `[38]` 组**（6 条）：把「同一个目录的多种写法」（`/tmp/x` vs `/private/tmp/x`）
  从"实现细节"升成"被断言的行为"。**前置条件本身先被断言**（若这台机器上两种写法恰好相同，
  整组会以全绿的样子什么都没测到 —— 假绿比红更贵）；另配反面对照：只提**父目录** ⇒ 不误拒，
  且确实写了进去（证明那条退 `0` 不是因为别的原因）。
- **自测第 `[39]` 组**（17 条，套件 37 组 371 条 → **39 组 394 条**）：专钉约数波浪号那个
  真机事故。判据层直接断言 4 个约数 / 错用户名 token 全 `False`，**并且必须让 cwd == 板**
  —— 假阳性只在这个条件下显形，换个目录整组会漏掉它；真坐标仍 `True` 作反面对照
  （证明不是"判据永远返回 False"）；CLI 层断言 `doctor` 不再退 `70`、两个入口退 `2` 而不是 `70`；
  最后一条是**别把判据做宽**：正常 `~/…` 必须照常展开。

### Changed

- **接入片段里不再出现引擎的绝对路径。** 它现在只写**看板目录**（本来就是数据、跟着板走）
  和板里的 `<看板>/worklog`。这是本版本**最重要的行为变更**，也是回归杀手锏所在：
  自测 `[36]` 断言片段里**不许出现** `/Desktop/work-log-` 或 `.workbuddy/skills/work-log`。
  写死路径的那一刻，这段协议在第二台机器上就是死链 —— 而且**静默失效**。
- 片段里新增一段「**名字要钉死**」的说明，并明确「**别把某个 agent 的名字写进共享文件**」：
  `AGENTS.md` 会被**任何在该目录启动的 agent** 读到，写死 `--agent a1` 等于给所有会话
  发了同一个身份。

### Fixed

- **`_fix_dash_values` / `KNOWN_OPTS` 漏登记了一批选项**（`--to` / `--rate-cap` / `--no-auto-ui` /
  `--port` / `--host` / `--no-watch` / `--set` / `--kind` / `--purpose`）。
  后果是 `--text --to` 这种"漏传值"会被**当成文本吞掉** —— 一个用法错误伪装成
  "提问内容就是 `--to`"。缺一个就等于给 `--text` 开了一个后门。
- **`identity set --force` 的语义在测试里被自己踩到**（`--force` 确实会把登记改到当前目录，
  于是"抢名字"的用例会把后面还要用的 fixture 改掉）。修的是测试：还回去，并另用一个一次性名字
  验证 `identity rm`。顺手补了 `rm` 的断言。
- **★ 判据按「字面拼法」比对，导致同一个目录的两种写法互不相认。**
  `onboard` 的拒绝判据与 `doctor ③` 的认坐标判据过去共用 `_path_forms()` ——
  它枚举 `d` 的写法（`str(d)` 与 `~/…`）再去文本里做**字面子串**匹配。可是
  `resolve_dir()` 总是先 `resolve()`，而手写协议里常是**未解析拼法**
  （macOS 上 `/tmp` → `/private/tmp`、`$TMPDIR` 的 `/var` → `/private/var`），
  于是同一个目录、两种写法，**永远匹配不上**。

  实测（2026-09-25）A/B 对照，同一份文件、同样内容、**只改拼法**：

  | 手写文本里写 | `doctor ③` 判定 |
  |---|---|
  | `/tmp/hole/board` | ✗ 一处都没有 |
  | `/private/tmp/hole/board` | ✓ 1 处齐备 |

  代价不对称，而且**两边都不报错**：`onboard` 本该拒绝「这里已有一份指向本板的协议」，
  实际退 `0` 并**静默追加出第二份互相打架的协议**（没人知道哪份是权威）；
  `doctor ③` 明明有坐标却判「一处都没有」，反过来劝人再写一遍。

  修法：新增 `_mentions_board(text, d)` 作为**唯一入口**，两级判据 ——
  ① `_literal_forms()` 字面快路径（不碰文件系统）；
  ② 把文本里每段像路径的 token 先 `os.path.samefile()`（按 inode + 设备号，
  一次覆盖符号链接、**大小写差异**（macOS 默认文件系统大小写不敏感，`resolve()` 归不了大小写）、
  挂载别名），不成就退回 `resolve()` 归一后比字符串（给还没建的目录兜底）。
  旧的 `_path_forms()` **改名**为 `_literal_forms()` 并在 docstring 里写明"别直接用它做判据"
  —— 换名字是为了让旧写法不可能被无意保留：它现在会 `NameError`。
  刻意仍**不认"尾巴相同"**（只认 `/.workbuddy/work-log` 会命中任何一块板），
  并配了反面对照：只提父目录 ⇒ 不误拒。
- **`onboard --to auto` 的手写协议预检也跟着受益**（它复用同一处实现），
  于是「一处被拒 ⇒ 一个字节都不写」这条在符号链接场景下才真正成立。
- **★ 约数波浪号被当成路径 ⇒ `doctor` 退 `70`，并且顺手埋了一种静默假绿。**
  中文行文里的 `~90%` / `~2.4s` / `~0.9-3s`（**约数**）被 `_PATH_TOKEN_RE` 的裸 `~` 分支
  吃成路径 token，而它展不开（`Path("~90%").expanduser()` 抛 `RuntimeError` ——
  **不是** `OSError`，所以 `except OSError` 接不住）。两个后果，第二个才是要命的：

  - 异常没被接住 ⇒ 一路冒到 `main()` 变成 **`70`（内部错误）**。真机
    `~/.workbuddy/MEMORY.md` 里正好写着这三行（`~90%` / `~0.9-3s` / `~2.4s`），
    于是 `doctor` 整条炸掉 —— 一个约数把体检打成"工具坏了"。
  - 只吞异常**更坏**：展不开会**原样保留**，`resolve()` 把它当**相对路径**接到 cwd 上，
    变成 `<cwd>/~90%`；**板恰好建在当前目录**时与板前缀匹配 ⇒
    一句「延迟 ~2.4s」就把这份记忆判成"提到了本板"。那正是 `_literal_forms()` 注释里
    立誓要避免的「永远为真的废话」，而且**不报错**。

  A/B 反证（同一段文本、板 = cwd）：

  | 实现 | `_mentions_board` 的结果 |
  |---|---|
  | 修复前（裸 `~` 正则 + 不接异常） | 抛 `RuntimeError` ⇒ CLI 退 **`70`** |
  | 只吞异常、不检查"展开后是否绝对" | **`True`** ⇒ `doctor` 判「✓ 1 处齐备」并退 `0`（假绿） |
  | 现在 | `False` ⇒ 判「⚠ 只提到工具」并退 `1` |

  修法不是给约数加特例，而是把"这看起来**确实**像个路径"变成**硬前提**，两层都要：

  ① `_PATH_TOKEN_RE` 的 `~` 分支要求**后面就跟分隔符**（`~/x` 或 `~用户名/x`）——
  裸 `~`（板恰好在家目录）刻意**不认**：判据宁可漏、不可猜；
  ② 新增 `_expand()`：展不开返回 `None`（**不原样保留**），且判据只接受**绝对**结果 ——
  「`resolve()` 把相对路径接到 cwd 上」正是假阳性的根源。

  代价刻意选成**漏而不是猜**：漏了只是多跑一次 `onboard`，猜了是一条没人会发现的假绿。
  同类修正一并用在两个命令行入口上 —— `--dir` / `--to` 里写展不开的 `~`
  现在退 **`2`（用法）** 而不是 `70`（以前用户把路径写错一个字，得到的却是"内部错误"）。

### Docs

- `examples/doctor-output.txt` 与 `examples/onboard-output.txt` 用**现行引擎重新捕获**
  （第五幕、第六幕、`docs/preview.html` 内嵌的正文同步换成新的；片段内嵌 `<!-- -->`
  在 HTML 里必须转义，否则会被浏览器当注释吃掉）。
- `docs/preview.html` 新增**第七幕「换台电脑、换个 agent，这段协议还能用」**：
  固定名字的三档解析、抢名字被拒的真实输出、引导脚本的查找顺序、退 70 的修法清单，
  以及「把 PATH 收窄到 `/usr/bin:/bin` 再测」这条 —— **假绿比红更贵**。
- `SKILL.md` 新增「名字怎么固定（三个会话别撞进同一个身份）」「换台电脑 / 换任何 agent」
  两节 + 命令表补 `identity` / `whoami`（22 → **24** 个子命令）+ 落点表新增 `auto` 行与
  「落点不用你判断：`onboard --detect` / `--to auto`」一节；`references/protocol.md`
  新增「身份」「可移植」「落点探测」三节 + 文件地图补三个新文件 + 退出码表补 5 行
  （含 `--to auto` 探测不到退 `1`、以及为什么它**不是**用法码 `2`）。
- 断言 **325 → 394**（新增自测 `[36]` 22 条 + `[37]` 24 条 + `[38]` 6 条 + `[39]` 17 条），
  测试组 **35 → 39**；相关数字在
  `README.md` / `README.en.md` / `docs/VALIDATION.md` / `docs/LAUNCH.md` / `docs/PUBLISH.md` /
  `CONTRIBUTING.md` / `SKILL.md` / `references/protocol.md` / `.github/workflows/test.yml` /
  `docs/preview.html` 十处同步。`docs/VALIDATION.md` 补第 `[37]` / `[38]` / `[39]` 组、
  结论表新增「落点探测」与「判据的'不许猜'边界」两行、并单独记下第 37 组的**两处**
  刻意的环境收窄（`HOME` 沙箱 + `PATH` 收窄）与第 39 组的**前置条件断言**
  （若哪天 `pathlib` 不再对 `~90%` 抛错，那一组会以全绿的样子什么都没测到）。

---

## [0.6.0] — 2026-09-24 · 把「接入」补齐两端：生成端 `onboard` + 自查端 `doctor` + 告警退避

主题：这套工具**最讽刺的一种失效**不是引擎坏了，而是协议从来没到过某个 agent 手里 ——
板建好了、`serve` 起上了、协议也写进了**对方**仓库的 `AGENTS.md`，然后自己**断了 42 小时心跳**。
根因是 `AGENTS.md` **只对在该仓库启动的会话生效**，cwd 在别处的 agent 永远读不到它；
而写协议的人自己在同一份文件里就写了「workbuddy 的 skill/记忆不在本仓库里」——
**知道洞在哪，却把协议写在了洞的另一侧。**

后来发现这条战线上「接入」其实是**两件**事，而它们的自动化程度**恰好相反**：

- **机械接入（工具侧）本来就是自动的。** `post` 对未知 agent 名会**当场注册**（`_get_agent`），
  根本没有 `join` 子命令；它那一次心跳让 `others` 非空，顺带触发 `auto_ui`
  （起 `serve` + 弹浏览器，而 `serve` 默认自带看门狗）。**这一半不需要改任何东西。**
- **认知接入（它那侧）不可能自动。** 模型没有推送通道 —— 上下文只在它**自己**发起工具调用时
  才更新。唯一办法是让义务句落进它**启动时一定会读到**的文件里。

所以本版本补的是**交付那一段**：`doctor` 负责「我有没有被拉起来」，`onboard` 负责
「把该报什么名、往哪报、多久报一次，写成一段能直接粘贴的协议」。
**两端共用同一道门槛**（`_path_forms()`），这样才不会出现「自己开的药、自己验不过」。

### Added

- **`onboard` 子命令 —— 把「接入片段」交到第二个 agent 手里。** 默认**只打印不落盘**
  （连看板目录都不会顺手建出来：该命令的 `--dir` 解析走新增的 `resolve_dir(..., mkdir=False)`，
  否则一个号称 dry-run 的命令会顺手 mkdir，那就不叫 dry-run 了）。
  `--to user|repo|claude|<路径>` 才写，写之前先备份、失败即中止（不留半个改动的文件）。
  - **幂等**：内容包在 `<!-- work-log:begin -->` … `<!-- work-log:end -->` 之间，
    重复跑只替换块内、块外一字不动 —— 所以它可以反复跑，也能跟着仓库进版本库。
  - **拒绝覆盖手写协议**：目标里已有「指向本板的异己协议」时**默认拒绝并退 `2`**，
    要追加必须显式 `--force`（两份互相打架的协议比不做更糟）。
    - ⚠️ 判据**不能**只认字面量 `work-log`：自测 `[35]` 抓到的真洞 ——
      实测手写的那份只写了「看板：`<路径>`」，**压根没提工具名**，只查工具名会把它静默追加成两份。
      现在改成「给了本板坐标（`_path_forms`）**或**提过工具名」。
  - **它产出的片段必然能通过 `doctor ③`** —— 片段天生带「坐标 + 报到义务」，
    且与 `doctor` 共用同一道门槛。这是刻意设计：**开药与验药必须用同一把尺**。
    回归组 `[35]` 把这条钉住了，并配**反向对照**（把坐标留下、拿掉义务句 ⇒ 那个 ✓ 必须立刻掉下来），
    以证明那个绿不是碰巧为真。断言 307 → **325**（+18）。
  - 刻意**不做**「自动发现对方是谁、它读哪个文件」—— 那要靠翻对方二进制里的字符串（`SKILL.md` 那一步）。
    **猜错落点比不猜更糟**：它会产出一段看起来完备、却永远到不了对方手里的协议，
    也就是本版本主题里那次失效。命令只承诺一件事：给我一个落点，我给你一段一定验得过的协议。
  - 备份内容直接用**已经读进内存的原文**写回，不再多引一个模块去 copy：
    本工具零依赖那条断言（`[18]` 白名单）是真护栏，不为了省事放宽；顺带也消掉了读盘与备份之间的 TOCTOU 窗口。

- **`doctor` 子命令 —— 把「接入 4 步」对称地用在自己身上。** 只读，五项体检：
  ① 同一项目有没有第二块（废）看板；② 当前 cwd 有没有指令文件；③ **每会话被注入的记忆文件里
  有没有这块板的坐标**（这条才是决定性的）；④ 板上各人的 `last_seen`（把「我」单独标出来）；
  ⑤ 告警 ÷ 实质发言的比值（远大于 1 ⇒ 真信号已被噪音埋掉）。
  退出码与协议一致：**有病 `1`**，板不存在（用法问题）`2`，不伪装成业务码。
  - ③ 的匹配刻意**不**用 `/.workbuddy/work-log` 这种尾巴 —— 那会命中任何一块板，
    使「我有坐标」变成一句永远为真的废话；改为认本板的两种书写形态（绝对路径 / `~/…`）。
  - ③ 的判据随后**又收紧一档**（同日追加）：不再只查"记忆里有没有本板坐标"，而是
    **坐标 + 报到义务 + 落点层级**三层（`DUTY_MARKERS` / `_is_global_mem`）。
    起因是一个**实测到的洞**：坐标写进了用户级记忆、**义务句却只写在项目级**
    ⇒ 换个工作区开新会话就断；而只查坐标的旧判据照样判 ✓ —— **判据自己看不见这个洞**，
    这比洞本身更值得记。断言 304 → **307**。
    - 「跨工作区那份里没有它」刻意**不**计 `bad`（当前工作区确实连得上，不算现在就失效），
      但必须把警示打出来，并由 selftest 断言那行文字存在 —— 否则它就只是装饰。
    - `DUTY_MARKERS` 刻意**不收**「报到」这类词：它太容易出现在**诊断句**里
      （"我这条线没有在报到"），会把"有病"判成"有义务"。**假阴性可以接受，假阳性不行。**
  - ① 的判据也不能只看目录名：废板的目录名取的是**当时那个 cwd 的 basename**，
    与现在的板常常不同（实测：桌面那块叫 `YoLoHoLoYo`，活板的 cwd 却在别处）。
    所以先比 `cwd`，再退一步比「板上是不是同一批人」。

### Changed

- **告警退避（P0 噪音治理）。** 原先只看 `alert_open` 就永远按 `COOLDOWN` 重复，
  实测某板 42 小时攒出 **994 行 watchdog vs 110 行实质发言** —— 「另一个 agent 真的停了」
  这件事被彻底淹没。现在连续重复 `ALERT_BACKOFF_AFTER(3)` 次后按 `135s × 2^k` 退避
  （上限 `3600s`）：**同一场景从 ~1120 条降到 ~45 条**。
  - 退避基数**刻意与 `--cooldown` 解耦**（只取 `max`）：否则有人把 cooldown 调成 0，
    就等于把退避也一起关掉了。
  - 措辞分档：静默 2 分钟说「疑似卡死」（值得去救），静默数小时说「**离线**」（人已经不在了）。
    两者处置动作相反，共用一句话会让人去救一个不存在的进程。
  - 新增 `fmt_dur()`：告警里的时长从 `152778s` 变成 `42h30m` —— 静默时长正是选处置动作的依据，必须可读。
  - 恢复心跳时 `alert_repeat` 一并归零，否则它下次静默会直接从「离线」档开始。

### Fixed

- **`check_stdlib_only.py` 把自己崩成了「发现违规」。** 它的 `TARGETS` 是相对 cwd 的路径，
  换个目录调用就抛 `FileNotFoundError`，而解释器此时恰好退 **1** —— 与「真的引入了 `requests`」
  长得一模一样（一边假违规，一边真违规被淹没）。现在包根按**脚本自身位置**推断，
  任意 cwd 都能跑；找不到文件 / 解析失败改退 **2**，并明说「这是检查器自己没跑起来」。
  顺带：这是本项目「工具跑不起来，绝不该伪装成业务结论」那条铁律的第 N 次复现。

- **`examples/demo.sh` 会留下"孤儿"serve 与临时目录 —— 也就是「跑完自动清理、不留痕迹」是假的。**
  两个独立缺陷叠在一起：
  ① 四处 `init` 都没带 `--no-auto-ui` ⇒「板上第 2 个 agent 上线」会**自己起一个后台 `serve` 并弹浏览器**，
  而 `cleanup()` 只杀它自己记下的 PID，那些 `serve` 全留下；
  ② 场景 ② 的两个后台 `await` 写成了 `run ... &` —— `run` 是函数，`&` 起的是一个**子 shell**，
  `$!` 拿到的是那个子 shell 的 PID，`kill $!` **杀不到真正的 python**；它变成孤儿跑满 30s timeout，
  在本脚本结束**之后**把刚删掉的 `deadlock/` 又写回来。
  实测清出 **3 个跑了 1 天 20 小时的残留 serve**（占着 8787/8789/8790）+ 若干只剩 `deadlock/` 的临时目录。
  修法：`init` 一律带 `--no-auto-ui`；后台调用去掉 `run` 包装让 `$!` 指向真进程；`cleanup()` 增加
  `pkill -f "$TMP"` 兜底（**必须判非空** —— `pkill -f ""` 会匹配所有进程）。
  **验证**：清空残留后从 0 跑一次 demo，跑前 / 跑后均为 **0 个残留目录 + 1 个 serve**。

### Docs

- 断言数 **290 / 32 组 → 325 / 35 组**，分两次收口，共 **11 个文件**精确对齐
  （README 中英、`banner.svg`、`preview.html`、CI 步骤名、`LAUNCH` / `VALIDATION` / `PUBLISH`、
  `CONTRIBUTING`、`SKILL.md`、`references/protocol.md`）。子命令数 **21 → 22** 同步落进
  README 中英的分组速查表、目录树与 `SKILL.md` 的命令表。**CHANGELOG 的历史条目一律不动** ——
  那些记录的是当时那一刻的真实数字，改了就是伪造历史。
- **`examples/doctor-output.txt`（以及 `preview.html` 第五幕内嵌的那份）与现行代码对不上了。**
  它自称「真实输出，没有美化过」，内嵌的却是 ③ 收紧**之前**的措辞
  （「记忆里的通道坐标：✓ 命中 2 处」）—— 也就是**一版已经不存在的行为**。
  「曾经真实」比明显编造更危险，因为它看起来可信。已用现行引擎重新捕获
  （③ 现为「坐标 + 报到义务」，并逐行标出 `[跨工作区／仅本工作区]`）；
  样例正文与捕获由脚本**逐行比对通过**才落盘，不是手抄的。
  顺带把开场那句「被查出 4 处没落好」改成 3 处 —— ① 的那块废板已在本版本内被移走。
- 新增 **`examples/onboard-output.txt` 与 `preview.html` 第六幕**（同样真实捕获 + 逐行比对）。
  写页面时踩到一个 HTML 细节：`onboard` 的输出里含 `<!-- work-log:begin -->`，
  **不转义会被浏览器当成注释整段吃掉** —— 内嵌 `<pre>` 必须走 HTML 转义，
  且由脚本从捕获生成而不是手抄（手抄正好就是上面那条事故的成因）。
- `SKILL.md` 命令表新增 `onboard` 行；「接入另一个工具链」的**第 3 步**（手写协议）加了指路块：
  这段不用手写，`onboard` 生成且保证与 `doctor ③` 对得上；「自查」一节的第 ① 条同样加了这一句。
  `references/protocol.md` 新增 **「接入片段（onboard）」**一节（四个落点对照表 + 三条不变量 +
  与 `doctor` 同尺的原因），退出码表补 `onboard` 的 `0` / `2` 两档。
- `SKILL.md` 新增两节：「**自查：你自己有没有被拉起来**」（含 4 条可当场验的命令）、
  以及 `「在册」≠「在线」` 里的**字段语义表** —— `last_seen`（判在线只看它）/ `entries`（历史总行数）/
  `posted_ts`（**速率限制的滑动窗口，不是终身计数**）三者含义不同，读错就会得出反结论
  （实测：`last_seen` 是 42 小时前、`entries=44` 却像很活跃）。
- `references/protocol.md`：告警状态机补退避分支；退出码表补 `doctor` 三档。
- `docs/VALIDATION.md`：「覆盖了什么」表原先只列到第 **29** 组，30–34 组一直是空缺 ——
  本轮补齐（新增「告警退避」与「接入自查 `doctor`」两行），并在顶部结论表加一行 `doctor`。
  **一个漏了两轮的表格，比一个漏了两行的表格更值得记一笔。**
- `docs/preview.html`：新增**第五幕「接入自查」**，数据取自新增的
  [`examples/doctor-output.txt`](examples/doctor-output.txt)（真实 `doctor` 输出，本机路径占位化）。
  demo 的四幕不动 —— `doctor` 属于「接入自查」，与前三幕的「故障检出」不是同一类叙事，
  硬塞进 `demo.sh` 会把 `demo-output.txt` 的"完整捕获"语义弄脏。README 中英的场景叙述随之
  由「四幕」改「五幕」，`docs/LAUNCH.md` 中英长文案各补一段「最后一个坑是坑我自己」。

## [0.5.2] — 2026-09-23 · 修好「第一个命令就崩」的演示 + 一串文档/包体漂移

主题：README 让新人做的**第一件事**（跑 60 秒演示）在 macOS 上**跑到一半就崩、退出码 1**；
顺带查出一串漂移：文档写着旧数字、仓库名大小写不对、源码包与已装版不同步。

### Added
- **新增 `docs/preview.html` —— 效果预览页（不用装、不用跑就能看懂）。**
  一页把四个场景画成图：看门狗抓静默卡死 · 等待图抓「心跳全绿的死锁」· 通信熔断叫停乒乓 ·
  多路等待任一达成。数据全部取自 [`examples/demo-output.txt`](examples/demo-output.txt) 的
  **真实输出**，页内自证来源，不美化。
  - 单文件、零第三方依赖、**完全自包含**（0 处外部资源引用）、离线浏览器打开即看 ——
    与项目本身「零依赖」的卖点一致。
  - 渲染用 headless Chrome **逐场景截图核对过**（生成只显示单个场景的临时副本放大检视），
    修掉两处：熔断框里「退出码」那行因 `display:inline-block` 挤进了上一句；
    多路等待图里「还在忙」的两条连线颜色过暗，几乎看不见（改亮 + 虚线，与"已回应"的实线区分）。
  - README（中 / 英）各加一节「先看效果 / See it first」+ 文档索引表各加一行；
    英文版 `Repository layout` 的 `docs/` 说明同步 —— 新增文件时**表格和目录树都要跟**。

- `SKILL.md` 新增「「在册」≠「在线」：对方不响应时的三分钟判定」一节：一条 `state.json`
  自查命令 + 四行判定表（`entries=0` 且 `posted_ts=[]` 且 `first_seen==last_seen` 且
  `note` 写着「由 init 预注册」⇒ **从未接入**，不是"忙"）、两条现场补充证据
  （`ps -Ao pid,lstart,etime,command` 看它在干什么；grep 对方仓库的 `AGENTS.md`
  看那边有没有接入配置）、两个最易误判的坑，以及结论口径。
  - 坑一：**一次性调用不产生心跳** —— `opencode run …` 这类被 harness 驱动的调用
    每次都是新会话、跑完即退，那个 agent **永远不会**出现在看板上。要心跳必须有
    **持久交互会话**在按循环走。
  - 坑二：**接入说明写在 A 仓库、它在 B 仓库干活 ⇒ 读不到** —— `AGENTS.md` 只对
    **在该仓库启动**的会话生效。
  - 口径：查完**如实说**「对方从未接入」，别写成"它可能忙""再等等" ——
    那会让人一直等一个不存在的对端。`await` 超时返回 `1` 的语义是"对方活着但没答"，
    **不覆盖**"对方根本没在听"这一种。

- **CI 新增一条静态护栏**（`.github/workflows/test.yml`）：扫描所有 `*.sh`，禁止
  `$变量` 后紧跟非 ASCII。`bash -n` **查不出**这类错（语法完全合法），只有真跑才会炸。
  护栏做过**双向冒烟**：仓库现状 `rc=0`；喂一个含 `$rc（` 的样本 `rc=1` 并打出文件:行号。

### Fixed
- 🔴 **`examples/demo.sh` 在 macOS 自带的 bash 3.2 下崩在半路（本轮最重要的修复）。**
  两处 `dim "…（$rc）…"` 把 `$rc` 紧挨中文标点写，bash 3.2 会把标点首字节吞进变量名
  → `line 91: rc?: unbound variable`，`set -u` 当场中止。**后果比"报个错"严重得多**：
  演示只跑到 ②（等待图抓死锁）就停了，③ 通信熔断 与 ④ 多路等待 **根本没执行**
  —— 实测修复前 **58 行 / `rc=1`**，修复后 **89 行 / `rc=0`**，四节齐全。
  这正是 0.5.1 在 `selftest.sh` 里修过 **6 处**的**同一类**写法，当时漏了这个文件。
  两处均改为 `${rc}`。

- **`assets/banner.svg` 里的断言数写的是「32 组 284 条」**，与 README / VALIDATION 的
  **290** 自相矛盾 —— 而它是 README 最顶上那张图，第一眼就能看到。
  （另有两处 `284` 是 SVG 里的**坐标** `y="284"`，不是数字声明，未动。）

- **CI 步骤名里的数字是陈旧的**：`回归断言（29 组 249 条）` → `（32 组 290 条）`。
  这个数字写在**工作流文件**里，最容易在加测试时被忘掉 —— 属于"改了一处、忘了另一处"。

- **源码包与已安装版逐字节对齐。** 比对结果：`scripts/work_log.py`、`assets/viewer.html`、
  `references/protocol.md`、`scripts/llm_agent.py`、`scripts/check_stdlib_only.py`、
  `scripts/selftest.sh` **全部一致**；唯一漂移是 `SKILL.md`（源包 40234B → 42323B，
  **39 行纯新增**，源包侧 0 行独有）。本次以已装版为准同步，此后不再有
  「实现已改、文档未跟」的漂移。

- **`.gitignore` 里有一条空转的规则。** 写的是 `worke-log/`（拼错），而工具真正使用的目录名是
  `work-log/`（`scripts/work_log.py:39` 的 `DEFAULT_DIRNAME = "work-log"`，全源码里
  **不存在** `worke-log` 这个字符串）。于是"在仓库根目录试跑"产生的 `work-log/`
  从来没被忽略过 —— 一条看起来在工作的规则，实际一个文件都没匹配到。已改为 `work-log/`。

- **仓库名大小写错了：文档里一律写成小写 `work-log`，而仓库真名是 `Work-Log`。**
  GitHub 的网页路径与 `git clone` **都不区分大小写**，所以本机照样克隆成功 ——
  但**克隆出来的目录名**是 `Work-Log/`，而文档后面写的是 `work-log/scripts/...`，
  在 Linux（区分大小写）上直接断掉；macOS 上则**完全看不出问题**。
  共改 17 处：`README.md` ×3、`README.en.md` ×3、`CONTRIBUTING.md` ×1、
  `docs/LAUNCH.md` ×5（那里承诺"可直接粘贴"，占位符必须换成真链接）、
  `docs/PUBLISH.md` ×5（仓库名、remote 地址、badge、清单）。
  同时把 `git clone` 一律改成**显式给出目标目录**（`… Work-Log.git work-log`），
  让"仓库名 `Work-Log`"与"工具名 `work-log`"不再互相污染。
  口径固定为：**URL / 仓库名一律 `Work-Log`；本地目录与命令一律 `work-log`。**
  `docs/PUBLISH.md` 的 §0.1 / §0.2 标为**已完成**，并补了一节讲为什么大小写要较真。

### Changed
- **面向新人的可读性（`README.md` / `README.en.md` / `CONTRIBUTING.md`）。**
  - README 里**一处都没写 UI 的地址** —— 用户实际问出过「我 UI 监看界面呢」。
    现在「30 秒上手」第 3 步标出默认 `http://localhost:8787`，并补上那个坑：
    `init --no-auto-ui` 会关掉「第 2 个 agent 开工自动起界面」，而且**没有反向开关**
    （只能改看板 `state.json` 的 `auto_ui`）⇒ 界面不会自己出现，得手敲 `serve`。
  - 「60 秒看到效果」补一句：**已在本地有代码就跳过 clone**，直接 `bash examples/demo.sh`。
  - selftest 耗时写的「约 3 分钟」与实测不符 → 改为「本机 M1 实测 1m34s」。
  - 「开发与自测」补「改了 `*.sh` 要**真跑一遍**」，并说清 `bash -n` 为什么不够。
  - **`README.en.md` 补上它缺的两节**：`Command reference`（20 个子命令速查）
    与 `Documentation`（文档索引表）—— 此前只有中文版有，英文读者根本看不到命令清单。
- **命名统一到 `work-*`**：`examples/demo.sh` 的临时目录前缀 `worke-demo` → `work-demo`，
  与 `scripts/selftest.sh` 的 `work-selftest`、技能名 `work-log` 对齐。
  （`.gitignore` 里那处 `worke-log/` 同理，见上。全仓现已无 `worke-` 前缀的命名残留；
  字面 `grep -rn worke` 仍会命中 `llm_agent.py` 与 `protocol.md` 里的 `worker` /
  `no_available_workers` —— 那是描述**自建网关的普通词**，不是命名残留，别误改。）
  这不只是观感：`worke-` 这个前缀只出现过**两处**（`.gitignore` 的 `worke-log/`
  与 `demo.sh` 的 `worke-demo`），两处都跟技能真名 `work-log` 不一致 ——
  留着会让后来者怀疑它是不是某个约定，而它什么也不是。

- **`docs/PUBLISH.md` 的占位符已填成真实值。** 这份手册原先是通用模板，7 处写着
  「你的名字 / 你的邮箱 / 你的用户名」（第 58、59、81、110、114、156、215 行），
  照抄得先手动替换一遍。现按本机 `git config --global` 的**实测值**填写：名字
  `YangLiHaoLiuYing`、邮箱 `YangLiHaoLiuYing@outlook.com`、GitHub 账号同名，
  命令可直接复制粘贴。
  - 例外一：**§5 Gitee 镜像整节标为「本机没有 Gitee 账户，跳过」**，那行地址
    **没有**填真名 —— 本机只有 GitHub 账户，填成 `gitee.com/YangLiHaoLiuYing/Work-Log.git`
    是一条**根本推不通的命令**，填了比留占位符更坏。现恢复为 `<你的Gitee用户名>`
    并在节首写明"本节现在不需要做任何事"。
    **教训：填真值前先确认那个账号真的存在** —— `git config --global` 只证明你有 GitHub 身份。
  - 例外二：§0.1 / §0.2 里作为「当初该怎么改」示例出现的 `<YOUR NAME OR GITHUB HANDLE>`
    与 `你的名字` **故意保留** —— 那两节讲的就是「占位符长什么样 → 换成什么」这个映射，
    换成真名反而读不懂。

- **`SKILL.md` 的示例里带着作者私有项目名。** 「30 秒上手」那条 `init --task "…"`
  写的是**本机一个私有项目的真实名字** —— 技能发出去等于把作者的项目名一起带上
  （同一段下面还跟着那个项目内部的模块路径）。已改为中性示例。
  **两版同步改**（源码包 + 已安装版），改完 `diff` 逐字节一致。
  - 注意：本条**故意不复述那个名字** —— 第一次改时我在 CHANGELOG 里把它原样写了一遍，
    等于"清理了 SKILL.md、又从 CHANGELOG 漏出去"。**改私有信息要连"修复说明"一起清**，
    否则说明本身就是泄漏点。

### 验收（本机实测复现，非引用历史结论）
- `bash scripts/selftest.sh` → **通过 290 · 失败 0**（耗时 1m34s，修完 `demo.sh` 后重跑）
- `bash examples/demo.sh` → **`rc=0`，①②③④ 四节齐全**（修复前：`rc=1`、只有 ①②、
  且在 ② 处中止）。行数在 89~90 之间抖动 —— 看门狗的异步 tick 会多打或少打一行，
  **别把行数当断言**。
- `python3 scripts/check_stdlib_only.py` → `扫描 2 个文件，共 27 处 import` / `ok: 全部来自标准库`
- 新增护栏双向冒烟：仓库现状 `rc=0`；坏样本 `rc=1`
- `.github/workflows/test.yml` 用 `yaml.safe_load` 解析通过（9 个 step）

---

## [0.5.1] — 2026-09-22 · 把「等待图」画出来 + 两处静默缺陷

主题：**这个工具相对其它多 agent 方案唯一独有的能力，此前在页面上一个像素都没有。**

### Added
- **看板渲染等待图。** `hazards`（不可达等待 / 互相等待）此前只在 CLI 与 `--json` 里可见，
  页面上完全没渲染 —— 也就是「心跳全绿却已经死锁」这件事，恰恰在最该看见它的地方看不见。
  现在：顶部常驻红色险情条（**不受筛选影响**，险情不该被一个 tab 藏起来），
  agent 状态表新增「在等」列（多路 `await` 会列出全部 id）、静默进度条
  （相对 `stale_after` 的比例，比一个裸秒数更快看出"快到阈值了"）、发帖频率过热提示，
  头部新增「⚠ 危险等待 N / ❓ 待回应 N」计数。
- **「↓ 回到最新」按钮。** 之前滚上去看历史之后只是**默默不再跟随**，页面上既没提示、
  也没有回去的路 —— 用户会以为看板卡住了。
- UI 细节：吸顶高度改为**运行时量取**（原来写死 `57px`，窄屏 header 换行时会把筛选栏
  压在它下面，内容被盖住）；筛选按钮带条数；`/` 聚焦输入框；险情与提问用 `aria-live` 播报。

### Fixed
- **`serve` 把 `viewer.html` 缓冲在内存里**（启动时 `read_bytes()` 一次），
  于是改前端必须重启服务才生效 —— 实测现象是：改完文件，页面上怎么刷都是旧版，
  `curl` 到的还是 19082 字节的老文件，很容易误判成"我改的地方没生效"。
  改为每次请求读盘（文件十几 KB，代价可忽略）；读取失败时返回一张说明页，
  而不是让整个看板报 500。
- **`selftest.sh` 在 macOS 自带 bash 3.2.57 下跑不完。** 6 处 `$var` 紧跟中文标点
  （`（rc=$rc）` / `（$probe）` / `$desc」`×3 / `（…口 $UIPORT）`），`set -u` 会把中文标点的
  首字节吞进变量名 → `line 78: rc?: unbound variable` 当场中止。
  全部改成 `${var}`（bash 3.2 与 5 都安全）。
- **`--help` 两处默认目录文案与实现不一致**：写着 `<cwd>/.workbuddy/work-log`，
  实际落点是 `~/Desktop/work-log/<当前目录名>`（见 `default_log_dir()`）。文案改对 ——
  这条特别坑：agent 的 bash cwd 通常在仓库里，而看板会落到**工作区之外**，
  每一步操作都要弹权限确认，心跳协议当场就死了。

### Changed
- `assets/viewer.html` 19082 → 26795 字节；页面维持**零外部依赖**（不引任何 CDN 样式或脚本），
  并由自测钉住。
- 自测新增 6 条 UI 「能力钩子」断言：`id="hazards"` / `hazardsHtml` / `waiting_ids` /
  `id="jump"` / `measureStick` / 无外链 —— 谁把等待图渲染删了，测试当场红。
  这几条不是"测 HTML 长什么样"，是钉住**后端算出来的东西有没有人在显示**。
- `SKILL.md` 新增「接入另一个工具链的 agent（跨工具协作，实测过一遍）」一节：四步流程
  （先 `lsof -a -p <PID> -d cwd -Fn` 查对方 bash 的**真实 cwd**、再决定看板放哪里；
  从对方二进制里**证明**它会读哪个指令文件而不是猜；`AGENTS.md` 只写协议不写教程；
  长命令 `hold`、收工 `--done`），以及两个必须先告知用户的坑
  （`/init` 会覆盖 `AGENTS.md`；**已经开着的会话不会捡起**新写的它）。

### 验收
- `bash scripts/selftest.sh` → **通过 290 · 失败 0**
- `python3 scripts/check_stdlib_only.py` → 全部 import 来自标准库

---

## [0.5.0] — 2026-09-19 · 多人协作感知 + 用户对话通道

### Added
- **多人协作感知（本工具的启动条件）。** "在干活" = 已开工、未收工、静默未超阈值（心跳中 / 挂起中）。
  `check` / `status` 的人类输出新增 🤝 协作行，`--json` 新增 `collab` 字段，`watch` 的 tick 行
  新增「在线干活 N」，`serve` 启动横幅与页面顶部徽章实时显示协作状态。
  第 2 个 agent 开工的那一刻，看板自动写入一条 `[协作] …多人协作模式开启`（仅一次）；
  全员停止后写入「结束」，之后可再次触发。
- **用户成为对话一等公民。** `用户` 是人类保留身份：agent 可以
  `ask --agent <你> --to 用户 --text "…"` 向人提问，人用
  `reply --agent 用户 --id N --text "…"` 直接回答，提问者的 `await` **原生**等到这条答复
  （不需要任何新机制）。`status` 会列出所有问向用户的待答提问及回答命令；
  agent 不能叫「用户」注册或发言（退 2，防冒充）。
- **人机对话豁免乒乓熔断。** 人跟 agent 的来回不是"两个模型互相确认"——
  与「用户」的连续对话不进通信过热的计数与告警。
- **serve 视图**：顶部新增协作徽章（🤝 协作中 / N/2 在干活），帮助区补用户回答方式。

- **自动协作 UI（理想流程闭环）。** 第 2 个 agent 开工的那一刻，post 自动
  以脱离会话的方式起一个 serve（日志落看板目录 `serve.log`）并打开浏览器 ——
  用户不用敲任何命令就能看到 agent 们在干什么。每次协作 session 只做一次
  （全员停止后复位）；端口被占自动后移；`init --no-auto-ui` 或环境变量
  `WORK_LOG_NO_AUTO_UI=1` 可整体关闭，`WORK_LOG_NO_UI=1` 只关弹浏览器。
- **UI 内直接交流。** `serve` 新增写通道 `/api/say`（页面输入框回车 = 向全体
  agent 喊话）与 `/api/reply`（点问题 chip 回答，提问者的 `await` 立刻拿到）。
  只开放这两个口，不提供任意命令执行；写路径与 CLI 共用同一实现并全程持锁。
  页面底部新增输入条与「回答 #N」快捷按钮。

### Fixed
- **serve 写通道加了三道跨站防线（重要）。** 之前任何你浏览的网页都能
  `POST /api/say` 冒充「用户」给 agent 下指令：`text/plain` 是"简单请求"，
  浏览器不预检直接放行。现在：非 `application/json` 一律 `415`；
  `Host` 不是 127.0.0.1/localhost 一律 `403`（防 DNS rebinding）；
  去掉了多余的 `Access-Control-Allow-Origin: *`（它让任意网页能读看板内容）。
  400/413 拒绝时断开连接，防 HTTP/1.1 keep-alive 字节流错位。
- **自动起 serve 健康检查超时会留孤儿进程。** 现在 2.5s 内没起来就 `kill -9` 再试下一个端口。
- **serve 写通道没有请求体大小上限。** 现在超过 64KB 直接回 `413`。
- **自动 UI 不再复用别的项目的 serve。** 探活从"端口有服务应答"升级为
  核对 `/api/health` 返回的看板目录——两个项目同时跑时，B 项目不会把
  A 项目的看板弹给你（串台）。
- **页面发言者昵称未清洗。** 现在去空白/冒号并截断到 12 字符，防一行被拆两行或伪装成别人。
- **页面全量重绘在大看板上卡顿。** 渲染上限 300 行（更早的折叠成一行提示），
  问题 chip 只在问题集合变化时重建；切到后台标签页时暂停轮询。
- **页面"滚动钉住"修复。** 滚动监听原来挂在 `#feed` 上，但实际滚动容器是
  document，`pinned` 永远为 true——看历史时会被新消息拽走/永远不跟随，
  两种症状取决于浏览器。现在监听正确的滚动容器。
- **页面性能：有提问挂着时不再每 2s 全量重建 300 行。** "已等 Ns"每秒都在变，
  原来它混在主渲染签名里；现在拆成易变签名，只重建 banner 那几行。
- **自测自己的时间炸弹：挂起过期断言原来把心跳硬编码成 `00:00:07`，
  每天零点后 45s 内会确定性抖红**（跨午夜实测踩到）。改用 `--stale-after 1`
  把阈值压到 1s，不再依赖"现在离零点很远"。

### 测试
- 自测 29 组 250 条 → **32 组 290 条**，新增 [30] 多人协作感知（9 条）、
  [31] 用户对话通道（10 条）、[32] 自动协作 UI / 页面写通道（19 条），
  Python 3.9.6 与 3.13.12 各自全绿，6 路并行压力 6/6 无抖动。
- 人类「用户」不再被注册进 agent 监督表（此前 UI 回答会把人变成受监督 agent、污染协作数）。

---

## [0.4.1] — 2026-09-19 · 收尾复核

### Fixed
- **用法错误不再借用业务码 `1`（重要）。** 引擎里 17 处前置条件错误原本写
  `sys.exit("✗ …")`，而 Python 对字符串参数默认退出 **1** —— 恰好又是协议里
  「超时 / 对方没回」的业务码。于是「我把编号写错了」和「对方不配合」在调用方看来
  完全一样，一个工具用法问题会被读成一条业务事实。现在统一走 `die()` → **`2`**，
  与 argparse 的用法错误码一致。
  - 附带发现：回归套件里 **13 条断言原本就写着「期望 1」**，等于把这个 bug 当规格焊死了。
    已全部改为语义断言（不仅要求 `2`，还把「退成 1」单独报成失败）。
- **`check` 现在把协作险情计入退出码。** 之前 `[互相等待]`（真死锁）会被检出、会写告警，
  但 `check` 仍退 `0` —— 脚本里 `check || 告警` 会把它整个漏掉，而它偏偏是这套机制
  最想抓的那类故障。现在「有卡死**或**有险情」都退 `1`，并补了反向断言
  （险情消失后必须回到 `0`）。

### Added
- 回归组 `[29]` **退出码契约**：锁住常量表 `0/1/2/3/4/70`、逐条扫描 18 种坏调用、
  静态护栏禁止 `sys.exit("字符串")` 复现，并反向确认真超时**仍**返回 `1`。
- `scripts/check_stdlib_only.py` + GitHub Actions 工作流：CI 自动跑依赖检查
  （零第三方依赖是这个项目的卖点，必须靠机制守）、250 条断言、60 秒演示。
- 驱动层把 `rc==2`（用法错误）与 `rc==70`（工具坏了）翻译成人话 ——
  否则模型只看到原始 stderr，照样会误读成"对方不配合"。

### Performance
- **看板解析 3× 提速。** `board_entries` 原本对每一行调用一次 `datetime.strptime`
  （占单轮 97%）。改为「日期段算一次基准 + 整数加法」：2000 行看板
  **34.3ms → 11.4ms**。所有需要扫看板的命令（`post`/`check`/`brief`/`await`/`watch`/`serve`）都受益。
- 回归护栏改成**数 `strptime` 调用次数**（≤8，实测 0）而不是数秒 —— 数秒会随机器负载抖。

### Changed
- 退出码常量集中到引擎顶部 `EXIT_*`，并写明「为什么 `1` 不能被借用」。
- 文档：`SKILL.md` / `references/protocol.md` 的退出码表重画为
  「业务码 / 用法错误(2) / 基础设施(70)」三块。
- 自测：29 组 **250** 条断言（原 28 组 222 条），Python 3.9.6 与 3.13.12 各自全绿。

---

## [0.4.0] — 2026-09-19 · 把预算从"互相客套"里救回来

### Added
- **通信熔断（乒乓）**：同一对 agent **连续交替**来回（同一个人连发两条不算一轮）
  达 `6` 轮开始提醒，达 `12` 轮 `ask`/`reply` 直接拒绝写入（退出码 `4`），`--force` 可覆盖。
  非交替的批量对话不会被误判。
- **心跳预算**：单个 agent 每分钟 `post` 上限（`init --rate-cap`，默认 60，`0` 关闭）。
  超线拒绝写入（退出码 `4`），且**不刷新 `last_seen`** ——
  于是「刷屏刷到卡死」的 agent 会同时被熔断和看门狗各抓一次，而不是靠刷屏续命。
- **多路等待**：`await --id 1,2,3`（默认全部必须回）+ `--any`（任一回应即达成）。
  未达成者**全部已收工**时退 `3`，并打印已拿到的答复与还缺谁。

### Changed
- `--id 0` 被**彻底拒绝**（退出码 `2`）。它曾经是「等任何新动态」的合法写法，
  但编号一丢就变成静默假成功。想要那个语义就**不写 `--id`**。
- `await` 的三个出口语义冻结：`0` 拿到 / `1` 超时 / `3` 对方已收工。

### Fixed
- **内部异常不再伪装成业务结论。** 新增退出码 `70`（sysexits `EX_SOFTWARE`）：
  引擎自己崩了必须与「对方没回」分开，否则调用方会拿一个软件 bug 去下业务结论。
  （起因：一次改动残留变量名导致每次 `await` 都崩，而崩了返 `1`，症状看起来像"对方全都没答"。）

---

## [0.3.0] — 2026-09-18 · 等待图

### Added
- **等待图（本项目的核心差异点）**：`await` 期间把等待关系写进共享状态
  `state.agents.<n>.awaiting`，看门狗据此检出两类**心跳全绿也发现不了**的故障：
  - `[不可达等待]` —— 你在等的人已经写了「任务完成」，这个答案永远不会来
  - `[互相等待]` —— 双方互等，谁都不会先答，是真死锁
- `await` 对已收工的对端**立刻**返回 `3`，而不是把 `--timeout` 干耗完
  （实测：对端早已收工，白等满 60s 且全程无告警）。
- 险情告警落盘 + 冷却期去重；`--json` 输出暴露 `hazards[]` 与每行的 `waiting_on`。

### Notes
- 两条不变量：只采信「还在等且静默 ≤ `stale_after`」的标记；险情消失后 key 必须被清掉，
  否则会永久报同一个险情。
- 仍然测不出：「还在动但方向错了」与「等外部条件（CI / 下载 / 端口）」。

---

## [0.2.0] — 2026-09-18 · 真机验收

用真实模型驱动两个 agent 走完整协议跑了 3 轮（第 1 轮因驱动层 bug 作废）。

### Fixed
- 修掉 **4 个驱动层 bug**，它们每一个都伪装成「模型不听话」：
  工具参数的对象壳/字符串壳、`await` 缺 id 时静默退化成 `--id 0`、超时预算算错、
  出口文案把 `3`（已收工）说成超时。
- 驱动层把「缺 id / 非正整数」当硬错误交回模型重试。

### Validation
- 双向问答全部闭环；决定里能引用对方原话；面对与已定结论冲突的用户要求，
  agent 走「阻塞 + 协商 + 显式折中」而不是沉默或嘴上答应；看门狗全程 **0** 误报。
- 自测 123 → 144 条断言。

---

## [0.1.0] — 2026-09-17 · 首版

### Added
- 共享文本看板 + `post` 心跳；`watch` 看门狗（静默超阈值且未写「任务完成」→ 告警）。
- 定向问答 `ask` / `reply` / `await` / `ack-user` / `brief` / `read-user`。
- 资源锁 `lock` / `unlock` / `locks`（避免两个 agent 抢同一文件 / 端口 / GPU）。
- 用户喊话通道 `say`，agent 认领后别人看得见「有人在管了」。
- 浏览器实时视图 `serve`；`hold` / `release` 声明长任务（期间不判卡死）。
- 零第三方依赖，单文件引擎，Python 3.9+。

---

## 契约冻结说明

以下内容在 `0.x` 期间也**不会**做不兼容变更：

- **退出码语义**：`0` 成功 / `1` 业务（超时、有卡死或险情）/ `2` 用法错误 /
  `3` 对端已收工、抢锁冲突 / `4` 熔断 / `70` 工具自身 bug
- **看板行文法**：`<agent> HH:MM:SS [标签] 正文` 与 `## YYYY-MM-DD` 日期分节
  （这是历史看板能被反解的前提）
- **三个 CLI 入口的相对位置**：`scripts/work_log.py`、`scripts/selftest.sh`、`SKILL.md`
